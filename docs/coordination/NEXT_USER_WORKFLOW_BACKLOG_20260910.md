# Rivune next user-workflow backlog — September 10, 2026

Scope: three user-visible gaps after the accepted `5d71ab110175f53101ff4b82dc1f6c6dbc9c1c4b` Tauri source and the proposed durable saved-result foundation. **RIVUNE APP BUILDER** remains the sole owner of production source, builds, and native delivery. This is a prioritization handoff; it does not change product source, run a provider, launch or replace an app, start a server, sign, publish, or forward private QA.

## Current boundary

- `5d71ab` is an accepted one-file reading-surface source change. Its separate ReadingQA bundle is unlaunched, and its JavaScript/bridge bytes are unchanged (`docs/coordination/READING_SURFACES_SOURCE_BUILD_RECEIPT_20260910.md:3-9`). It therefore does not prove connection, send, follow-up, durable-result UI, restart, or release.
- Compact conversation search/show-all and the single compact composer configuration control already exist in parent `3bc19ce`; they are not backlog items.
- The durable artifact host foundation is a 13-file **unexported** workspace delta awaiting independent review. It exposes exact inert inspection but explicitly leaves pane wiring and native/provider acceptance undone (`docs/coordination/DURABLE_ARTIFACT_FOUNDATION_RECEIPT_20260910.md:3,17,29-31`).

## Ordered delivery slices

| Order | User workflow gap and source evidence | Smallest implementation slice | Acceptance | Owner and dependencies | Existing authorization gate |
|---:|---|---|---|---|---|
| 1 | **Connect one supported CLI and arrive back at a sendable first prompt.** The product requirement is to open Rivune, connect supported tools already available to the user, and get a usable default without terminal management (`docs/AI_TEAM_DIRECTION.md:18,30,33`). Current Settings can discover installed Codex/Claude routes and durably save one, but it explicitly does not establish sign-in or a response (`tools/symphony/source-import-candidate/prototypes/ai-native-workspace/src/host/ProviderSetup.tsx:35-48,62-71`). Guidance sends the user to the provider's own app/CLI, and a not-tested route merely tells them to return and send (`tools/symphony/source-import-candidate/prototypes/ai-native-workspace/src/host/connectionGuidance.ts:5-14`). | Turn existing Settings states into one bounded handoff: **Find installed providers → choose route → Save default → Continue to chat**. On durable save, close Settings only by explicit Continue, restore focus to Message, retain the draft, and show the selected route plus the honest state **Sign-in unverified** or **No successful response yet** beside the composer. If no route is found, keep the official-install/sign-in guidance and draft; do not add a terminal, installer, guessed provider, or automatic test call. | From an empty provider-configuration fixture, one installed supported route can be saved, appears as the workspace default, and returns the user to the unchanged first draft in one action. Missing, authentication-needed, not-tested, failed-response, uncertain-save, and display-refresh-failure states each have one truthful next action. Navigation/configuration performs no provider request. At 320px/390px and keyboard-only use, the selected route and Continue action remain reachable and focus returns to Message. | **RIVUNE APP BUILDER.** Reuse `ProviderSetup`, configuration-operation recovery, current catalog/readiness DTO, Settings opener focus, and the compact composer summary. No new adapter or Rust provider execution is required for the local slice. | Local source/fixture implementation is already within app-work authorization. A real authentication claim or first response needs the user to sign in with that provider and separate explicit authorization to run a live provider request. |
| 2 | **After a successful send, give the user a clean, durable follow-up box.** Rivune already assembles up to eight completed exchanges / 12 KiB into later request context (`tools/symphony/source-import-candidate/qa-artifacts/cross-platform-shell-20260907/candidate4-runtime-r2/src-tauri/src/host.rs:574-655`). The controller first saves the prompt as the conversation draft, submits it, then after acceptance resets renderer text to `currentConversation(...).draft`—the same saved prompt—rather than durably clearing it (`tools/symphony/source-import-candidate/prototypes/ai-native-workspace/src/host/workspaceController.ts:345-372`). This makes a normal second turn awkward and can revive a sent prompt after restart. | When the host durably admits the exact saved prompt as a run, clear that conversation's draft in the same authoritative host transition, preserving its provider/team/attachments policy and returning the new rich-draft revision in the authoritative snapshot. The controller adopts the blank draft only if the admitted prompt and local edit version still match; newer typing is never erased. Rejected or uncertain admission keeps the original draft. Focus Message after terminal completion/cancellation without auto-sending or auto-opening configuration. | Accepted send shows the user message exactly once in the transcript and an empty focused composer ready for a follow-up; restart keeps it empty. A second send includes the prior completed exchange through the existing bounded history and records a separate run. Rejection, reservation/journal failure, uncertain admission, refresh failure, cancellation, and edits made while submission is pending do not lose or clear unsent text. Reconciliation clears exactly once only when the saved run proves admission; it never replays the prompt. | **RIVUNE APP BUILDER.** Requires the host admission transaction/receipt, workspace snapshot revision, controller edit-version fence, recovery journal, and existing assembled-history tests. This is a host/controller durability slice, not a visual-only clear. | Local implementation and deterministic host/controller tests are unblocked. A real two-turn provider acceptance test requires explicit live-provider authorization; native close/reopen proof requires a separately scoped QA launch/profile. |
| 3 | **Make durable answers and Constellation contributions usable beside chat.** The goal requires one useful answer or completed piece of work and an exact readable result (`docs/RIVUNE_DESIGN_DIRECTION.md:13,21,91-101`; `docs/AI_TEAM_DIRECTION.md:18,35`). The accepted host still exposes only transcript text and modal SavedResult actions; `HostSnapshot` at `5d71ab` has no artifact collection (`tools/symphony/source-import-candidate/prototypes/ai-native-workspace/src/host/contracts.ts:18-21`; `tools/symphony/source-import-candidate/prototypes/ai-native-workspace/src/host/HostWorkspace.tsx:139-159`). The durable foundation adds immutable metadata and exact inert inspection but leaves the pane unwired. | After independent acceptance/export of the foundation, implement only the agreed V1 Results surface: one **Results (N)** control in the active conversation header, newest-first host-issued result rows, explicit selection, and an inert exact-text inspector with Back, Close, Copy, Select all, and retry-inspection. Use a right pane only when chat keeps at least 480px; on narrow layouts replace the chat region while keeping chat mounted/inert. Keep the existing transcript Inspect dialog until replacement behavior is independently accepted. No editor, file tree, terminal, preview execution, OS reveal, export, or managed provider files. | Ordinary final, partial Constellation member, and lead-synthesis records open only through their exact conversation/request/artifact/digest tuple. Same-name records, late A/B responses, retry races, unavailable/corrupt content, conversation switches, cancellation, and superseded versions never show stale text. Close/Back restore exact focus; draft and transcript scroll survive. Script/HTML/Markdown-like content stays inert and exact copy is explicit. The pane never saves, sends, changes configuration, or invents a result during uncertain persistence. | **RIVUNE APP BUILDER.** Depends on independent acceptance and source export of `DURABLE_ARTIFACT_FOUNDATION_RECEIPT_20260910.md`, then the interaction contract in `DURABLE_RESULTS_PANE_INTERACTION_20260910.md` and its synthetic race/focus cases. | UI implementation is blocked only by foundation acceptance/export. After that, local fixture implementation is authorized. Native inclusion/restart, OS clipboard proof, installed replacement, managed-file admission, signing, DMG, and publication each retain their existing separate gates. |

## Delivery order and stop conditions

1. Ship the honest connection-to-composer handoff first because no other workflow matters if the user cannot select a supported route and reach a sendable prompt.
2. Fix admitted-send draft clearing next so the first answer naturally becomes a second-turn conversation rather than a prompt-replacement exercise.
3. Wire Results only after the durable host boundary is independently accepted and exported; do not copy the browser demo's synthetic files or stale SwiftUI surfaces.

Stop and report the exact dependency if the chosen provider lacks a supported adapter, the route is not authenticated, admission remains uncertain, the durable-artifact foundation is rejected, or a native/release gate is required. Do not fill those waits with speculative IDE features, more settings categories, website expansion, or unsupported providers/models.

## Exact next dependency

RIVUNE APP BUILDER can begin slice 1 now against accepted `5d71ab`. In parallel within its sole ownership, it can prepare slice 2's host/controller patch and deterministic recovery cases. Slice 3 must wait for the assigned independent review of the unexported durable-artifact foundation and a named accepted source export.
