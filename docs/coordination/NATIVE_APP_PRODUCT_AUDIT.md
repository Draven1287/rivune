# Native app product audit

Owner: **Summarize current work**, reporting to **Audit Rivune native app**. Started September 6, 2026. Product decisions and implementation assignments go through the root audit task.

**Checkpoint: corrected 0620 is installed; AO05 artifact continuation, AO02 provider-status handling, Command-N focus, and fallback display ordering have bounded source/test/rendered acceptance.** The installed executable SHA-256 is `7c121b7e8b19a29297fb6deab5805910d5ff018dda43e5bbe10aca0879af8f48`; the final 45-file source manifest matches and the test result is 297/0/0. CUA is released. This remains a local ad-hoc build: no whole-app acceptance, live-provider quality improvement, live authentication, notarization, or public release is claimed.

## Identity, isolation and evidence

| Item | Verified identity or boundary |
| --- | --- |
| Installed baseline at audit start | 0.2 (2026090618); executable SHA-256 `885efabc70eef4c63b96d2add18c0414c98cc6acf0c52b742874fe83b621de94` |
| Frozen source | All 44 mirror files matched the [0618 manifest](../../qa-artifacts/team-foundation-20260906/build-0618/source-manifest.json), SHA-256 `5e3218c8bb853f97d9f363adfe490caba351e06aea29bcd0d1535d6a749a2617`. Mirror: `/private/tmp/rivune-review-2026090618/source` |
| Isolated app actually launched | `/private/tmp/rivune-native-product-audit-0618/Rivune-Audit.app`; unique bundle identifier `com.aaravshah.rivune.audit0618`; `RivuneUIPreview=true` |
| Clone correspondence | Only bundle identity/display metadata and ad-hoc signature changed. Copies of installed and audit executables, with signatures removed only from temporary comparison files, have the identical SHA-256 `2fb07a9b11b6850426f0a9ade4e498fb9376efe16d86b8c61ceffd6c55bf934d` |
| Clone executable | Signed audit executable SHA-256 `468ace8e7da060cd6495d72045a8363f2926f69c0795eb6442ca52c60f08d49b`; [isolation receipt](../../qa-artifacts/native-product-audit-20260906/frozen-0618/isolation-receipt.json) |
| Storage | Ordinary preview chat/project/draft/team storage is memory-only; unique bundle identity separates standard appearance preferences. Preview mode is not an operating-system sandbox |
| Actions actually performed | Explicit-path clone launch, Home accessibility-tree read and Home screenshot only. No navigation, typing, send, login, sign-out, file apply/revert, install or real-app control in this rendered checkpoint |
| Screenshot boundary | Home image was displayed inline by CUA in this task's transcript. No screenshot file was saved; this record does not invent one. Selected AX observations are transcribed below |
| Preserved source-harness evidence | [AO-05 results](../../qa-artifacts/native-product-audit-20260906/frozen-0618/ao05/results.json), [extraction manifest](../../qa-artifacts/native-product-audit-20260906/frozen-0618/ao05/extraction-manifest.json), [execution receipt](../../qa-artifacts/native-product-audit-20260906/frozen-0618/ao05/execution-receipt.json), [copy receipt](../../qa-artifacts/native-product-audit-20260906/frozen-0618/evidence-copy-receipt.json). All 23 copied evidence files matched their source hashes; executable not copied |

Preview suppresses onboarding/startup and evaluation flows. Ordinary send is guarded, account startup and bridge/listener startup are disabled, and API form save is disabled. Underlying API services and project file-apply methods are not universally isolated: no credential injection, API calls or real project writes are permitted by this audit method. These restrictions prevent treating a preview-only observation as live onboarding or provider acceptance.

## Five improvements to prioritize

The identifiers from the [account/output source audit](ACCOUNT_OUTPUT_PRODUCT_AUDIT.md) are retained to avoid duplicate assignments. No P0 was established in the bounded checks; this is not a conclusion that the unexercised app has none.

| Priority / identifier | Improvement and impact | Evidence state | Fix ownership and acceptance |
| --- | --- | --- | --- |
| **P1 / AO-05** | Bind follow-up edits to the selected artifact revision or project snapshot. A visible complete file currently need not be complete in the next request | Reproduced in frozen 0618 exact/extracted builders and recording transports; detailed measurements below. Actual rendered selection/restart and live completion are unexercised | Root prioritizes after 0619 and assigns implementation. Selected full files must reach the next request, or the UI must block with an explicit file/context selection action. Preserve immutable revision identity and external-edit conflict checks |
| **P1 / AO-02** | Explain provider failures with a specific supported recovery action while preserving the prompt/files | Companion audit has source evidence for generic recovery. No real failure was triggered here; source version fingerprints remain in that audit | Root prioritizes after 0619 and assigns adapter/UI owner. Fixtures distinguish supported error categories and unknown, preserve draft/files, expose no secret output and never auto-switch or make an additional call |
| **P1 / AO-04** | Offer a bounded, explicit repair action for unusable generated-file output | Source-supported companion finding. The valid AO-05 fixture does not exercise malformed-output repair | Root places after AO-02/AO-05. Preserve original response; require an explicit repair request, revalidate all files and reject unsafe/oversized output before any write |
| **P2 / NP-01** | State that Swarm is unavailable in the Home control itself and its accessibility value | Rendered Home and AX observation on frozen 0618; source mechanism confirmed in `WelcomeView.swift:103–143` | Root assigns Home/control owner. Show an accurate visible unavailable state and AX value; connection readiness must not imply the disabled workflow becomes available after login. Keep execution disabled until the separate Swarm milestone is accepted |
| **P2 / NP-03** | Focus the new conversation composer after Command-N | Reproduced in isolated corrected 0619: shortcut opens Home, AX focus remains on the standard window, typing does not populate Message; clicking the field then typing works | Root queues after AO-05 unless owner finds a trivial disjoint correction. Shortcut-created conversation must focus the intended empty composer and accept typing while preserving prior drafts, without submitting or stealing another window’s focus |

Account-footer and separate-Settings-window complaints are already assigned to the 0619 increment. They remain one existing work group, not additional defects or duplicate implementation requests. The [S01–S11 matrix](AI_TEAM_INDEPENDENT_REVIEW.md) covers their acceptance, including legacy appearance preservation.

## AO-05 reproduction and measured result

The subagent used frozen source builders, the extracted frozen `ResponseArtifact` parser, full frozen `CouncilRunner` and inert recording transports. The synthetic response contains `index.html` with 32,000 bytes and `style.css` with 1,001 bytes, with beginning/middle/end sentinels. The parser accepts the artifact. Memory is enabled for the ordinary follow-up case.

| File | Original content | Included in ordinary follow-up | Lost content |
| --- | ---: | ---: | ---: |
| `index.html` | 32,000 B | 11,929 B | 20,071 B |
| `style.css` | 1,001 B | 0 B | 1,001 B |
| Total file contents | 33,001 B | 11,929 B | 21,072 B |

The complete JSON response is 33,124 bytes. The 12,000-byte history retains 11,951 original JSON bytes, losing 21,173 JSON bytes; envelope bytes and file-content bytes are separate measurements. The beginning sentinel remains, but the middle/end/CSS sentinels do not. **Zero complete files remain.** One direct recording call and three Council recording calls accept the context without an artifact-specific block. Council's fixture result completes; it is not a measured model answer.

The clipped newest-history case includes an internal `[truncated]` marker in the model history. This does not identify the selected artifact revision or restore missing files. With one newer short exchange, the older oversized artifact is omitted entirely: the history is 57 bytes, with zero artifact bytes and no truncation marker.

An explicit manual snapshot provides a bounded alternative: a 14,000-byte file becomes a 14,869-byte composer prompt and reaches the protected request intact. The 33,001-byte two-file snapshot instead creates a 33,924-byte prompt, exceeding the native 16,384-byte prompt guard. The latter is a source size comparison, not a run through the app admission UI. Neither case proves automatic artifact continuation.

Source identities:

- `RivuneStore.swift`: `19b3bc48316d9ea0bcd06d17cd07a329e3faaa34877fe93eb561cbed1e6f5b96`; history construction at frozen lines 1995–2030.
- `ProjectWorkspace.swift`: `2b56b6da1811ce9a95b17112a3a3d9366434dbc225cb95be7bd1ee8ae1a078b8`; manual snapshot action at frozen line 719 and artifact viewer controls later in the file.
- `CouncilRunner.swift`: `ca8b244b1dd344c5a76fade0c7ec587b7fc9d0cde854a84e1379e46eb23b29de`.

All inspected frozen inputs remained unchanged after execution. [Recorded direct request](../../qa-artifacts/native-product-audit-20260906/frozen-0618/ao05/direct-next-request.txt), [Council draft request](../../qa-artifacts/native-product-audit-20260906/frozen-0618/ao05/council-next-draft-request.txt) and [older-artifact history](../../qa-artifacts/native-product-audit-20260906/frozen-0618/ao05/older-artifact-history.txt) preserve the received inputs. This reproduction does not establish app restart behavior, rendered file selection, an actual provider response, stale-disk overwrite or continuation quality.

## AO-02 isolated candidate review

Root requested an independent read of [recovery.patch](../../qa-artifacts/native-product-audit-20260906/ao02-proposal-review/recovery.patch), SHA-256 `3364f33a71cb38e431177276a35f35972893892ab0d3524baf6bd031e869ade1`, and its [README](../../qa-artifacts/native-product-audit-20260906/ao02-proposal-review/README.md). **Bounded source acceptance: no concrete new retry/fallback/admission or diagnostic-leakage defect found.** This is an isolated proposal, not shared-source integration or AO-02 closure.

The copied Terminal baseline exactly matches frozen 0619 SHA-256 `b78b67ffd65d15173674f61caf8668f0177d634b92b898af82f6719f66d00e31`. The patch changes status classification and removes broad generation diagnostic matching for authentication words. Subprocess arguments/call sites, timeouts, cancellation guards, request routes and Council retry policy remain unchanged. Startup selection still requires `isReady`: generic failed checks moving from signed-out to unavailable remain ineligible, and the existing pre-request API fallback policy is preserved. No other frozen-source caller branches on the two changed terminal error categories.

Recovery copy is static and the result/action is a finite enum; raw stdout/stderr does not enter these fields. Claude sign-out requires successful exit plus strict Boolean `loggedIn: false`. Nonzero, malformed and oversized output stays unknown; Codex's prior exit-zero contract is retained rather than newly validated. Nonzero generation exposes only a fixed provider name and numeric status through the existing error copy.

The [24-fixture receipt](../../qa-artifacts/native-product-audit-20260906/ao02-proposal-review/fixture-results.txt) was read, not rerun: no concrete concern justified repeating it. It covers pure classification/action cases; timeout and missing-executable checks are pure mappings, not real subprocess or filesystem exercises. App typecheck/link, live CLI-version contracts, installed behavior and rendered action acceptance remain unverified.

**AO-02 remains open.** The classification proposal alone projects the enum to readiness, so its new message/action is not surfaced. A separately supplied [initial UI proposal](../../qa-artifacts/native-product-audit-20260906/ao02-proposal-review/recovery-ui-initial.patch), SHA-256 `344ad164191285c52f80370bae98ade91f37ff147c225d15451140520d0c3405`, now adds Not verified/static explanation/Check connection to first-run CLI rows. It remains unintegrated and unrendered; other connection surfaces are outside this minimal proposal.

**P2 / AO-02-RefreshGuard — closed for revised candidate source; integration/rendering pending.** The initial source-supported finding follows for provenance. The new action advertises that it refreshes configured connections, but its `isChecking` guard at candidate Settings lines 1595–1596 tests only the two CLI states. If both CLIs finish while an API probe is still pending, an unavailable CLI row's Check connection button becomes enabled. `RivuneStore.refreshConnections` only guards preview isolation; `launchConnectionChecks` cancels the current task and starts all routes again. The new action therefore permits a second refresh during the advertised all-connection operation. This inherits an existing predicate gap; it is not an observed live request or an automatic retry.

The revised [candidate patch](../../qa-artifacts/native-product-audit-20260906/ao02-proposal-review/recovery-ui.patch), SHA-256 `30e76d6f5a6f029eb7772fdd98bfaf2f14e95b6341ae5efb1ca355dc7f6d3fdd`, now checks full `startupPhase` in both its disabled state and action guard. Candidate Settings SHA-256 independently matches `5ce09bfdf6356ac04f91b8e8ddfde41c24e24e63bc9930dee4a79be306dbd79b`. Production source sets checking synchronously before dispatch and completes after all route checks and capability collection.

The [9-check receipt](../../qa-artifacts/native-product-audit-20260906/ao02-proposal-review/ui-fixture-results.txt) and [extraction script](../../qa-artifacts/native-product-audit-20260906/ao02-proposal-review/check_refresh_guard.rb) were read without a rerun. They exercise the exact candidate action/disabled expressions against a recording store that models the synchronous phase transition. CLI-finished/API-held, immediate second invocation, completion and preview guards pass. This closes the reported candidate guard sequence; it does not exercise actual concurrent provider tasks, rendered accessibility, draft preservation or app integration. Root and candidate owner received the bounded acceptance.

Static recovery copy, explicit initiation and the absence of new on-appear/chat-send behavior remain supported. Updated [UI notes](../../qa-artifacts/native-product-audit-20260906/ao02-proposal-review/UI-NOTES.md) retain app typecheck and rendered acceptance as outstanding. The [preservation receipt](../../qa-artifacts/native-product-audit-20260906/ao02-proposal-review/preservation-receipt.json) records the initial patch reconstructed by restoring its two changed lines, with its exact original hash matched. AO-02 overall remains open, and root retains implementation assignment after 0619 acceptance.

## AO-02 integrated source checkpoint

The [integration handoff](../../qa-artifacts/native-product-audit-20260906/ao02-integration-review/HANDOFF.md), frozen copies and [results](../../qa-artifacts/native-product-audit-20260906/ao02-integration-review/results.txt) are preserved. All four frozen and shared-workspace hashes independently matched at review: Terminal `43060dcbdf58cd8ff4c17d36f3c96ef49bedd9a75d83c8f1ec562ef10eb194cf`, helper `063bb7322539bc9e1bde94293f92729097b3ce70beada54b3ccf5cc4b830e806`, Settings `5ce09bfdf6356ac04f91b8e8ddfde41c24e24e63bc9930dee4a79be306dbd79b`, and new XCTest `5517582cb9e4bc9e0c52beadc3b405a3f7a64f10c01267e95d8a3cb8be536861`. The three implementation files exactly match the reviewed corrected candidates; no additional implementation difference needs a duplicate fixture run.

The new XCTest translates the 24 classification/action cases into XCTest assertions. It does not add actual connection concurrency, filesystem discovery, timer, UI or model-request tests. The owner reports rerunning24 helper and 9 extracted UI guard recording checks plus four Swift syntax parses; this reviewer read the receipt without rerunning it. Native target registration and full app build/XCTest execution remain with the native owner. No new implementation blocker was found in this comparison.

Evidence caution sent to root: the translated XCTest prints `PASS`/`24 passed` after nonfatal assertions, so those custom lines can appear even if an assertion fails. Canonical XCTest/xcresult status must be authoritative; the printed counter is not proof of24 passed native tests. The owner corrected this reporting issue in [test-reporting-correction](../../qa-artifacts/native-product-audit-20260906/ao02-integration-review/test-reporting-correction/review-receipt.json): new XCTest SHA-256 `7bbc9cd4d5d71bce637302b49251af9063cc24211b401a55a4153a6d1da148ee` independently matches frozen/shared source. The diff removes only success printing/counter and adds assertion descriptions; all 24 scenarios remain. The reporting concern is closed at source level, with no new execution claim. AO-02 overall remains open. Installed 0619 does not contain this subsequent source integration.

## Home observation and NP-01 reproduction

Open the isolated 0618 clone at its initial Home screen and inspect the mode selector visually and through accessibility. The following observations were read from CUA, not inferred from source:

- Sidebar exposes Home, Conversations, Projects, Connections, search, two synthetic recent conversations and Settings. No account identity footer is visible in this baseline. Its addition is already assigned to 0619.
- The Home screenshot presents the dark galaxy background, central prompt, Council/Swarm/ChatGPT/Claude selector, Team · 2 and Think/Build/Review starters. No obvious clipping appears at this initial window size; narrow-window or contrast acceptance does not follow.
- Council is selected. Swarm is disabled and visually muted, with visible text `Swarm`. Its AX value is `Connection required`, while AX help says `Agents build different parts of a project · unavailable`. Thus unavailable information exists in help, but the visible label and primary status do not make the distinction clearly.
- Composer has a labeled message field, Add files, Model configuration, Permissions, voice input and disabled Send. The empty-send help is `Enter a message first`; the visible connection notice says to review the team and connect every selected CLI. These are observed labels, not keyboard or provider behavior acceptance.

Frozen `WelcomeView.swift` SHA-256: `0266d318ea714cbb10723f381ee590138eabb6653449966160468a11fac2a02c`, independently read. The control uses `mode.displayName` and assigns `ready ? "Ready" : "Connection required"` even though `.swarm` is always disabled. Acceptance should test both its visible state and its AX value, preserving the useful unavailable help.

## Initial 0619 source checkpoint while UI is handed off

The independent source subagent matched all 44 frozen files before/after review to the [initial 0619 manifest](../../qa-artifacts/native-settings-20260906/build-0619/initial-source-manifest.json), SHA-256 `41805d529ee766f8f535780d70801ea694d3f045064c55a194a1647bb38f77e7`. The initial candidate executable matched staged SHA-256 `26d4b5f62e89bff82554393399d5ccd5d67b6ee087733152e2a6723a44db848b`. At that review, the exported test summary recorded **278 passed / 0 failed / 0 skipped**; no test rerun occurred here. Those first-stage results do not identify the later corrected candidate merely because its build number is also 0619.

The owner's QA clone has unique bundle ID `com.aaravshah.rivune.qa0619`, preview enabled and executable SHA-256 `f2d4de51c04e706382d5524ba0ca6f9e534a29ab6f3c88bdf8ad29e6467d8343`. This review does not launch it or promote the 0618 Home observation to 0619.

- **S01/S02:** Source removes the separate Settings scene and shares `presentSettings` entrypoints, retaining the mounted workspace. The new synthetic test preserves selected conversation/project, draft and pending run identity. Attachment and actual mounted UI preservation remain separate.
- **S03:** Escape is the declared Back to workspace shortcut. Focus restoration is guarded by the same key window, no attached sheet and a surviving responder; failed `makeFirstResponder` has no fallback. This is a target for rendered testing, not a reproduced defect.
- **S04:** The pending-run test completes its synthetic runner but makes no subsequent result/exactly-once assertion. That test does not close completion while Settings is open.
- **S06–S09:** Footer projects app-account state, requires a nonempty verified identity, suppresses uncertain/busy identity and does not equate CLI readiness with account auth. Its actions route to Settings; no new token fields are displayed. The requested state combinations, delayed A → signed-out → B and secret-sentinel display cases are not established by the two new tests.
- **S10/S11:** Missing/invalid preset keys preserve legacy galaxy/stars/dim. The [appearance receipt](../../qa-artifacts/appearance-presets-20260906/source-receipt.json) reports 77 pure assertions/typecheck for the exact Theme SHA-256 `f73e0e903be5af2892d0893746ba1e43dfa87a03df33724b0112af8d165ea026`. Its full Settings hash predates routing integration; this is component evidence, not integrated rendered/persistence acceptance.

A subsequent independent exact-source footer harness supplies bounded display evidence: **15 of 16 synthetic cases met the deliberately nonblank-ID contract**. Local/unconfigured, signed-out, verified display identity, pending/busy, failed verification, missing/empty ID and blank display identity behaved as expected. A synchronous A → signed-out → B sequence displays A → Local workspace → B; it does not exercise delayed asynchronous completion.

The one mismatch is a whitespace-only `verifiedUserID`, which is nonempty and therefore displays Verified. Frozen Sidebar line 355 does not trim that ID; the production Supabase backend emits `user.id.uuidString` at frozen Account line 104. This is a malformed-input defensive projection gap, not a demonstrated reachable authentication failure or installation blocker. Root received the measured limit. No credential was used and no secret-output acceptance is inferred from a projection that intentionally displays the account identity.

[Footer receipt](../../qa-artifacts/native-product-audit-20260906/frozen-0619/footer-projection/receipt.json), [all outputs](../../qa-artifacts/native-product-audit-20260906/frozen-0619/footer-projection/outputs.json) and [exact extraction](../../qa-artifacts/native-product-audit-20260906/frozen-0619/footer-projection/projection-exact.swift) are preserved. Sidebar SHA-256 stayed `fe955a1d837d94e0b4c48e3ae7f221c44fcf0979b98a85e5b2166eb5cc95cbce`; extraction SHA-256 is `b249d72e73592b12586aed1804de0db74843b3764633605c503ee0fd164f2479`. Only a pure Foundation command-line harness ran, with no app/UI/auth/provider activity.

The initial source check found no demonstrated new defect. The subsequent native-owner rendered check, relayed by root, reproduced the following issues before installation. These are owner-reported rendered findings, not this reviewer's UI replay; exact native evidence paths and the corrective manifest are pending.

| Finding | Reproduction and impact | Owner / corrective acceptance |
| --- | --- | --- |
| **P1 / S03-Focus** | Enter a synthetic composer draft, open in-main Settings, then Back. Draft text remains, but typing focus does not return to the composer. Continuing to type can fail to continue the intended draft | Native owner owns the Components/Root correction. After each sidebar/menu/Command-comma entry and Back/Escape return, typing must reach the intended preserved editor. Deferred focus must operate only in its own active app/key window and never steal focus from another app/window or attached sheet |
| **P2 / S11-PresetAX** | Appearance preset cards expose an unknown AX role. The control's action and selected state are not usefully expressed through the observed accessibility semantics | Native owner owns the small Theme correction. Presets must expose an actionable supported role, meaningful name and selected state, and support keyboard choice. Verify in the rebuilt rendered checkpoint |
| **P2 / S01-HiddenWorkspaceAX** | While Settings is active, the disabled underlying workspace still appears in AX despite the existing hidden modifier. Assistive navigation encounters content that is not the active screen | Native owner owns the Root wrapper correction. Hide only inactive workspace children while Settings is active; real Settings content must stay exposed/actionable. Returning must restore the active workspace's AX tree and preserved state |

All three belong to the existing 0619 Settings/appearance acceptance group. The corrected checkpoint review follows; the first staged 0619 build remains historical and is not the accepted candidate. CP4 remains closed.

## Corrected 0619 candidate review

**Final source/test/install binding verified; saved-rendered acceptance remains bounded to the captured states.** The corrected executable independently hashes to `c7e880b9f770a8dbe2e7f1dd35bba0d15ef012156eda406c476d5d7468c5128b`. The updated [44-file manifest](../../qa-artifacts/native-settings-20260906/build-0619/source-manifest.json) hashes to `b8fbe353d8cb011658d0fc9b29283c4414475dc8a22a404830d29ccf4e613db1`.

All 44 files match the owner's `/private/tmp/rivune-review-2026090619/accepted-source` and this reviewer's independently copied `/private/tmp/rivune-0619-final-independent-9fvvvghe/source`. The owner's original `source` directory intentionally retains the first stage and differs in Root, Theme, Components and coordinator tests; it is historical evidence, not a broken final mirror. The final installation receipt now binds this binary to the updated manifest and final 278-test result. The initial staging record and first-stage review text are historical; when read for this update they had not yet been relabeled.

The [final installation receipt](../../qa-artifacts/native-settings-20260906/build-0619/installation.json) identifies `/Applications/Rivune.app`,0.2/2026090619, preview=false and local ad-hoc signing. This reviewer independently read the installed plist and matched the executable hash above. The [final test summary](../../qa-artifacts/native-settings-20260906/build-0619/test-summary.json) records **278 passed / 0 failed / 0 skipped**, finishTime 1788755645.118; no suite rerun occurred here. The [owner history receipt](../../qa-artifacts/native-settings-20260906/build-0619/history-verification.json) records conversations, runs, drafts and projects JSON semantically unchanged; this reviewer read the receipt, not the real history. Rollback is `/private/tmp/rivune-review-2026090619/Rivune-replaced-0618.app`. This is local installed acceptance, not notarized distribution or live account/provider proof.

The reviewer independently matched all seven hashes in [root-rendered-review.json](../../qa-artifacts/native-settings-20260906/build-0619/root-rendered-review.json), viewed the three saved screenshots and read Settings/Home/chat AX receipts. This is saved-evidence inspection, with no direct UI replay by this reviewer.

| Finding / requirement | Corrected evidence and bounded verdict |
| --- | --- |
| S03-Focus | Saved Home AX contains `Draft before Settings. Continued safely.` with the message field focused. Saved chat screenshot/AX retains `Continue the API client.` and appends `Keep the interface small.`. **Observed Home/chat typing-return defect closed in these captured states.** Arbitrary selection ranges, multiple windows, app deactivation races and every keyboard path were not replayed |
| S11-PresetAX | Settings AX exposes Graphite/Orbit/Cosmos as buttons, with Orbit selected and explicit selected/not-selected values. Screenshot shows readable card labels and selection. **Observed preset-role defect closed in this captured state.** Full keyboard traversal, all preset persistence cycles and minimum-size/contrast measurement remain outside the capture |
| S01-HiddenWorkspaceAX | Settings AX contains active Settings navigation, search, section controls and content; the hidden workspace's chat/sidebar children are absent. A disabled native sidebar toolbar control remains. **Observed inactive-workspace-child defect closed in this captured state**, without hiding actual Settings content |
| S04 completion in Settings | Corrected test source asserts completion while Settings remains open, preserved draft/chat/project, one recording call, and after return exactly one completed record with the expected answer. This addresses the earlier missing-assertion gap. Final exported receipt now records 278/0/0, with installation binding to this manifest. S04 is closed at the synthetic coordinator/store test scope; no live provider or mounted-UI completion replay follows |

Source review finds that composer restoration captures its window and prior selection before Settings disables it, checks active app/exact key window/no attached sheet before restoring focus, rechecks active app/exact window after yielding, and clamps the previous range to current editor length. No app/window activation call is added. The generic Root responder fallback remains scoped to its original key window and a responder belonging to that window; these source checks do not replace app/window lifecycle testing.

The Root wrapper ignores/hides only the inactive workspace's accessibility children; Settings is a separate active sibling. Theme explicitly adds button and selected traits to presets. No new source blocker was established in this bounded correction review. Footer malformed-ID projection limits, missing live account-state combinations, the unfinished whole-app walkthrough and all release boundaries remain open. Native owner retains CUA until explicit release.

## Remaining 0619 walkthrough and inspection blocker

The native owner explicitly released CUA after final installation. This reviewer opened only `/private/tmp/rivune-review-2026090619/Rivune-QA.app`, with unique QA bundle identity and preview mode. The initial tool open took 476.9 seconds; subsequent normal actions completed in roughly a second. No installed-app control, global keyboard preference change, login, provider send, copy/share or real project file write occurred.

[Walkthrough observations](../../qa-artifacts/native-product-audit-20260906/frozen-0619/walkthrough/observations.json) are transcribed from this task's CUA outputs, not labeled as a raw AX export. The answer screenshot remains inline in the task transcript.

- Selected **A clearer API client**, entered `Audit draft A — keep exactly.`, switched to **Project browser states**, entered a separate B draft, and returned to A. The A draft was restored exactly. The initially empty B composer and its separate edit were observed; a later B-return was not performed.
- The synthetic final-answer screenshot exposes its fictional provenance, Swift code, comparison table and composer. Code/table content is present in AX. This does not establish all long-answer layouts or a minimum-window check.
- **NP-03 reproduced:** with A's composer focused, Command-N opens Home and clears the new Message field, but AX focus becomes the standard window. Typing `Audit new draft C.` leaves the AX tree unchanged and Message empty. Clicking Message then typing inserts it. The new-chat shortcut does not provide a usable immediate typing destination in this observed state.
- Opened Projects, observed its empty state and local/account/provider separation copy, opened New project, entered `Audit disposable project`, and observed Create become enabled. The project is an isolated memory-only fixture, with no selected files.
- Clicking Create returned `Sky Computer Use native pipe closed before response`. Project-detail creation/completion is therefore unverified. One AX retry, then one CUA session reset and explicit-path reselect with 20-second timeout returned the same error. No further recovery attempt is authorized by this audit checkpoint.

[Sanitized diagnostic receipt](../../qa-artifacts/native-product-audit-20260906/frozen-0619/walkthrough/inspection-failure.json) preserves hashes and excerpts of three matching **SkyComputerUseService** crash reports at 22:49:53,22:50:03 and22:50:40 MDT (04:49–04:50 UTC). Each identifies EXC_BREAKPOINT/SIGTRAP with `_assertionFailure` → `Array.remove(at:)`. This confirms an inspection-helper crash. This reviewer's process query was sandbox-denied; root separately verified installed RivunePID92658 and0619 QAPID95783 still running at 04:51:54 UTC, and found no Rivune-named diagnostic in the scoped directory. Process existence does not prove app responsiveness or project creation success.

CUA was explicitly released to root/native owner, with no pending operation. Remaining Team/fallback ordering, preset keyboard traversal, narrow-window and project/artifact interaction checks require a working inspection connection. At that blocked checkpoint, NP-02 remained a source hypothesis. The later recovered walkthrough below corroborates it visually; neither state establishes actual fallback runtime order. Root queues NP-03 after the AO-05 checkpoint; this reviewer makes no product-source correction.

## Recovered0619 bounded walkthrough

Root recovered inspection by terminating only the disposable0619 QA process, then verified fresh Home/Conversations inspection before handing CUA back. This reviewer reselected the exact QA path in0.1575 seconds and completed the following checks. The previous three helper-crash diagnostics remain valid historical evidence; the project-creation path was not repeated.

[Recovered coverage receipt](../../qa-artifacts/native-product-audit-20260906/frozen-0619/walkthrough/recovered-coverage.json) transcribes direct CUA observations. Screenshots were displayed inline in the task transcript, with no invented local screenshot path.

- **NP-02 is now rendered-confirmed:** adding Member3 and selecting Member2 then Member3 as fallbacks, then moving Member3 earlier, leaves the visible rows Member2(priority2) above Member3(priority1). The helper copy says fallbacks run in the order shown. Save/reopen retains the mismatched display order. This is an ordering/copy usability defect, not evidence that the runtime executes in the wrong order.
- Six members disable Add member. Scrolling exposes the last member, fallback controls and bottom Cancel/Save actions. Escape discards unsaved additions and returns to saved Team3. At the compact parent size, the three-member sheet remains scrollable with reachable bottom controls.
- **Preset keyboard coverage passes in this environment:** starting from Settings search, Tab traverses section buttons to Graphite, then Orbit and Cosmos. Space changes each selected state with visible focus retained. Graphite was restored. No global keyboard setting changed. This resolves the earlier unverified Tab/Space attempt without treating it as a reproduced defect.
- Focused Team selector opens with Space; Down/Return chooses Member2 in the open menu; Escape cancels the edit and Home retains Member1 as orchestrator. Return while the selector was closed activated the sheet's default Save action; no separate defect is inferred from that platform-dependent behavior.
- Native Window → Move & Resize → Left, then Top Left, produced AX width921 and a compact Home screenshot921×733. Home controls, the Team sheet, synthetic answer code/table, final-answer bottom clearance and contribution-details sheet were reachable. This is the observed native tiling size, not a claimed exact logical920×680 measurement. Return to Previous Size restored AX width1260.
- Both `Recovered draft A.` and `Recovered draft B.` returned exactly after chat switching through Conversations. These are same-process in-memory fixtures, not cross-launch persistence evidence.

CUA is explicitly released to root/native owner with no pending operation. QA remains on the synthetic Project browser states conversation, draftB, saved Team3, Graphite and restored window size. The installed user app was not controlled. Live account/provider states, actual generated-artifact interaction/restart, project creation completion, real failure/cancel/retry and full VoiceOver remain unqualified. The new stable AO-05 checkpoint is reviewed separately from this installed0619 UI.

## Coverage ledger and next handoff

| Area requested by root | This review's actual coverage | Outstanding check / dependency |
| --- | --- | --- |
| First-use onboarding and local use | Preview-isolation source reviewed; onboarding is suppressed in preview | Disposable supported first-use harness or separately authorized isolated environment. No live onboarding claim |
| Account versus provider state | Home notice observed; companion source audit and isolation review read | Fixture-backed footer/Settings comparison after 0619; no real authentication |
| Sidebar and navigation | InitialHome plus twochat switching and Projects navigation observed | Remaining navigation/search/Connections and return states; helper blocked after projectCreate |
| Settings entry and return | S01–S11 prepared; 0618 baseline footer absent | Native owner has exclusive CUA for staged 0619 acceptance. Review its receipt, then resume only after explicit release |
| Composer and drafts | RecoveredA/B eachrestoreexactlyafterConversationsnavigation; NP-03CmdNtypingdestinationfails | Attachments/restart/liveprovider preservation remain separate |
| Projects/files/artifacts | AO-05 recording reproduction; Projects empty/list and namedCreate dialog observed | ProjectCreate completion unknown after helper crash; rendered artifacts/save/snapshot workflows remain open |
| Team and workflows | RecoveredUI: Team3 save/reopen, renderedNP-02 mismatch, sixmemberreachability, Escape cancel and selector keyboardchoice | Actualfallbackexecution/livecapacity remains outsideUI fixtures |
| Answer presentation | Fictional Swift code/table/final answer screenshot and AX observed in0619 | Other long-answer/manifest states and minimumwindow layout remain; AO-01/AO-04 source gaps retained |
| Failure/cancel/retry | Existing CP4 recording evidence remains closed; AO-02 source gap recorded | Current UI fixtures showing failure and preserved input. Live provider failure remains unexercised |
| Keyboard/accessibility | PresetTab/Space and TeamSpace/Down/Return/Escape passed; NP-03CmdNfocus remains open | FullVoiceOver/otherapp-window lifecycle remain unexercised; globalsettingsunchanged |
| Narrow window | NativeLeft/TopLeft tiling:AXwidth921, Homeimage921×733; Home,Team3sheet,answerbottom/detailcontrolsreachable | No exactlogical920×680 or exhaustivecontrast/VoiceOver claim |
| Native window/focus | No new activation/minimize/full-screen test | Existing 0618 owner receipt is bounded historical evidence. Avoid duplicate 0619 owner checks; request explicit remaining coverage after handoff |

CUA is released to **Update Rivune product direction**; no further UI operation may start here until explicit release back. The audit remains in progress. Root recovered the helper and bounded additionalUIcoverage is complete; the failingProjectCreatepath was not repeated. Final 0619 source/test/install receipts are verified. Resume remaining coverage only with working inspection and an explicit UI handoff; do not repeat acceptedSettings/appearance cases without a new concern. CP4 IR-01 and bounded freshness findings stay closed unless relevant sources change.

## Frozen AO05/AO02 source review result

[Independent review receipt](../../qa-artifacts/native-product-audit-20260906/ao05-checkpoint-review/REVIEW.md): all 15 frozen hashes match; supplied native XCTest is 296/0/0. **AF02 P2 changes requested:** a null/non-object turn makes the whole-array cast skip invalid selectedArtifact metadata; fallback can then replace primary history. Exact extracted guard reproduced the bypass. Root/native owner notified with the targeted container-validation fix and primary/backup byte-preservation regression request. Other AF01–AF10 dispositions are bounded source/recording acceptance; rendered AO05 remains pending. AO02 registration/corrected XCTest is included; no new install or live-provider claim. Separate polish candidate excluded.

## AO05/AO02 v2 correction accepted

[Bounded v2 review](../../qa-artifacts/native-product-audit-20260906/ao05-checkpoint-review/v2/REVIEW.md): all 15 hashes verified against manifest `711c170ab0adce7f802516bdd2dcfc0a4d1f3b523b8cc3dd6891c93123cf97c1`; only Store guard and regression test differ from v1. Independent exact-extraction checks passed 8/8: six malformed-container cases preserve primary/backup bytes and block fallback/save; two healthy-primary precedence cases remain allowed. **AF02 P2 closed.** Supplied native XCTest is 297/0/0. Proceed to next local QA build; rendered AO05 and separate polish remain unreviewed. Installed0619 unchanged; no new UI/provider/install operation. Earlier v1 blocker entries are historical.

## Installed 0620 checkpoint

[Final build review](../../qa-artifacts/native-product-audit-20260906/build-0620/REVIEW.md) records the installed result. This reviewer independently matched `/Applications/Rivune.app` to build `2026090620`, version 0.2, executable SHA-256 `7c121b7e8b19a29297fb6deab5805910d5ff018dda43e5bbe10aca0879af8f48`, with no preview flag; `codesign --verify --deep --strict` passed. The final 45-file source manifest has zero workspace drift and the canonical test summary is 297 passed, 0 failed, 0 skipped.

Owner-rendered evidence covers artifact-only and mixed generated/ordinary file context, accurate counts and pre-send size wording, and removing both selected-file types while preserving the typed draft. Project consent is unchanged. Canonical conversations, projects, runs, drafts, and preferences are semantically unchanged. The recovery backup normalized one legacy `sharedPlan` phrase; its original is retained in `/private/tmp/rivune-review-2026090620/Rivune-replaced-0619.app`. Normal startup restored 24 conversations and showed both existing CLI connections ready without sending a request. These are local receipt and startup observations, not live model, semantic-output, auth, signed-distribution, or public-release acceptance.
