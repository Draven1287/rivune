# S02 persistent configuration operation recovery

Frozen isolated candidate `94fbdcefe9fcad64d3527bf88e4a24acec84ceb6`, parent `ae27a875aab6bc4d78f05f73e1374d73a5826171`. 125 files, tree SHA256 `07e98e12e47751ed2a39583d44902b44b7796926d05a0d2621c93eb7e5c01a85`. Ten-file delta in GIT_IMPORT_CANDIDATE.json; clean candidate, no remotes, all committed blobs match the manifest. Direct-user visual work remains excluded.

The existing private atomic workspace snapshot now owns one configuration operation and a monotonically increasing host-issued identity. Reserve precedes apply. Immutable intent includes provider/default choice and the original comparison guard; no browser journal, credentials, prompts or draft contents are stored. Apply requires exact identity/payload and commits provider configuration with applied state atomically. Already-applied same-ID requests re-establish persistence without applying again. Closed or replaced IDs cannot apply. Legacy configuration calls remain compatible when no unresolved operation exists and reject while recovery is pending.

Renderer mount reads the outstanding record and blocks another save; it never calls apply. Visible applied state remains unconfirmed until explicit reconciliation successfully persists it. A reserved operation reconciles to rejected and fences delayed application. Terminal acknowledgement permits a new explicit operation. A lost reservation reply is recovered from the host. Legacy bridges keep their earlier explicit path with a visible warning that they lack restart recovery. All five persistent bridge methods are required to use the new path.

Verification: four synthetic native recovery tests pass (lost-ack/reopen, exact ID/different payload, same-ID no-reapply, reserved restart/delayed apply fencing, before/after-rename faults, uncertain reconciliation, removed route reservation recovery and corrupt identity validation). Eight frontend adapter tests pass (four new recovery and four legacy configuration). Three selected mounted scenarios pass, including repeated renderer remount, no replay and rejected-reservation/display-refresh wording. TypeScript, native command registration compilation without launching, and nine source-stage checks pass. Logs: qa-artifacts/s02-configuration-recovery-20260910/native.log, frontend.log, renderer.log, registration.log. An initial parallel fixture reopen reported a profile-lock failure. Fixture paths now include a per-process atomic counter to guarantee uniqueness; final focused tests pass.

Review correction first: inherited admission P2 was separately corrected in parent ae27a875aab6bc4d78f05f73e1374d73a5826171. It resolves the current default through current catalog/defaults-only logic while preserving null saved selection and recording resolution provenance. See S02_INHERITED_ADMISSION_RECEIPT_20260910.md; one native regression plus two capability compatibility tests passed.

Next ready: independent review of persistent operation state transitions, uncertain persistence/acknowledgement and frontend remount fencing against this frozen commit. Native runtime integration, real provider/authentication, installation, publication and whole-build acceptance remain outside this verification. No product decision was needed for this bounded storage reuse.

Automatic approval review previously blocked cross-task forwarding. This receipt is local and no forwarding was retried.
