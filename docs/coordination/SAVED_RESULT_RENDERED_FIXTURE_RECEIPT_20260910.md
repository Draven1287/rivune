# Saved-result retained rendered fixture

URL: http://127.0.0.1:4317/tests/savedResult.html?scenario=saved-result

Sole preview owner restored the existing Vite service after lsof found no listener on4317. PID73902; cwd prototypes/ai-native-workspace; command node node_modules/vite/bin/vite.js --host127.0.0.1 --port4317 --strictPort. process.json and preview.log under qa-artifacts/saved-result-rendered-fixture-20260910 record ownership. Strict port prevents fallback to another port. One listener confirmed; no second server or native instance was launched.

Fixture entry requires exactly one scenario=saved-result parameter. Missing/other/repeated selection renders only an explanation. It does not load the host renderer tests or a provider/persistence bridge. Actual SavedResult.tsx/SavedResult.css/savedResultCopy.ts and the current user-edited global src/styles.css are imported. Test-only wrapper CSS is separate; galaxy/sidebar/product files are unchanged. Exact served fixture/component/global-style hashes: qa-artifacts/saved-result-rendered-fixture-20260910/source-hashes.json.

Includes completed A (65 scrolling lines, literal HTML/script/code, indentation, a1500-character line), distinct partial B and host-shortened member C. Clipboard is replaced in this document by a fixture-owned stub before rendering. It never calls the original clipboard. Default mode denies. Defer mode queues promises; buttons resolve/reject after closing, and Alt+Shift+R / Alt+Shift+J (physical KeyR/KeyJ) settle while another modal is open. Thus late resolution/rejection can be tested across result viewers without OS clipboard writes. No provider, persistence, or unfiltered suite call exists.

Basic validation: TypeScript passed; HTTP200; CUA rendered all three openers, opened partial B with its exact label/text and initial Close focus, and clicking Copy displayed the synthetic denial message with textarea focus. Subsequent lower-level DOM selection probes timed out, so full selection bounds/focus restoration and viewport acceptance are not claimed. Fixture remains served for independent review; no native app restart or publication. The requested390x844,1440x900,320x568 layout/focus/selection/deferred matrix remains the independent reviewer’s next step.

Source/state reports were read; this supplies their missing live fixture. No private QA forwarding was attempted.
