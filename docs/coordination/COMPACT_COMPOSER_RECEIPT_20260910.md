# Compact Single AI / Constellation composer

Implemented approved slice3 against COMPACT_COMPOSER_CONTRACT_REVIEW_20260910.md. Exact eight-file boundary: qa-artifacts/compact-composer-20260910/source-hashes.json. This is local source only; no export, native inclusion or independent acceptance claimed.

HostWorkspace mounts one ComposerExecutionControl instead of the consecutive ConstellationConfiguration and ConversationConnection disclosures, and removes the redundant action-row provider label. Saved snapshot state supplies the compact summary: Single AI connection, or Constellation member count and lead. Catalog labels fall back to saved IDs. Unsaved editor choices never supply this summary. Exact provider wording is “Provider default · exact model not reported”. The former components remain in source but are not mounted by HostWorkspace.

The bounded disclosure offers Single AI with explicit workspace-default or pin routing, and the existing Council team with2–6 available connections and a selected lead. No model, reasoning, Auto or Swarm menu. The editor scrolls in normal flow, leaving Message visible at320/390. Close/Escape discards local edits and returns focus; a confirmed save also closes and returns focus. The summary stays one visual line with full accessible text/title.

New configureExecution action captures the complete intended selection/team pair and an expected snapshot/catalog key before asynchronous work. It refreshes, rejects changed revision/default/catalog/routes/capability, revalidates exact current provider-default team structure and lead equality, and calls the existing save path once. That path preserves current draft and attachment IDs and requires the matching CAS receipt and refreshed saved state. Switching from a team to Single AI explicitly saves either null/null (follow default) or selected pin/null; it never performs two sequential mode/connection saves. Pending run/retry/submission and uncertain earlier-save fences remain enforced.

Dirty editor changes in saved configuration or catalog require Load saved configuration or Keep my choices. Missing saved providers remain visible by ID; unavailable teams are not silently cleared. An uncertain save retains choices and existing summary until recovery, using the same saved mutation. Existing host draft conflict resolution remains available. Past run labels, admitted team/provider provenance, partial member results, saved-result viewer and run retry/cancel controls remain separate and unchanged.

Validation details: qa-artifacts/compact-composer-20260910/validation.json. TypeScript passes. Four focused controller cases pass: explicit follow-default/former-lead/other pin transitions with one mutation and exact draft/attachments; stale/invalid input matrix before mutation; identical uncertain-save retry and frozen next send; existing partial-answer cancellation/restart preservation. Ten selected mounted checks pass at default desktop,320x568 and390x844, covering prior team conflict/acknowledgement behavior adapted to the new control, pin/inheritance, catalog changes and unavailable IDs, uncertain-save recovery, saved versus unsaved summary, Escape/save focus, and frozen historical partial output. The final default desktop run also passed all ten cases after restoring normal viewport sizing. No full unrelated/native suites were run.

Manual320 keyboard traversal reaches mode, each member, lead, Review connections, Save configuration, Cancel configuration, then Message without a trap. Escape from inside the editor returns to the summary. Screenshots were inspected at320 and390 with Message still visible below the bounded editor. Reviewers can use the selected retained synthetic URL in validation.json; the collapsible results rail reserves space rather than overlaying product controls.

Fixture corrections during validation: uncertainty must be injected before mounting because the bridge adapter captures its methods; Keep my choices resolves the conflict, so testing discard afterward correctly uses Cancel then reopen. Both corrected cases pass. These were test setup corrections, not suppressed product failures.

Preserved accepted conversation-list/result source, galaxy assets, global stylesheet and sidebar source. Scoped new CSS only affects the composer. Existing4317 only; no provider execution, real persistence, OS clipboard, new server, native build/launch/restart, release or private forwarding. Stop here for independent source and rendered review before further product changes.

## Duplicate-label P2 correction

Read COMPACT_COMPOSER_UI_REVIEW_20260910.md. Colliding provider display labels now append the stable provider ID in Single AI pin options, inherited/pinned descriptions and durable Single AI/Constellation summaries. Unique labels stay concise; missing entries still fall back to ID. Existing member/lead labels keep their ID without duplicating it. Collision comparison ignores case and surrounding whitespace. Selection values, controller, atomic save semantics and CSS are unchanged.

Only ComposerExecutionControl.tsx, composerConfiguration.ts and hostRenderer.test.tsx changed from the preceding eight-file boundary. Refreshed source-hashes.json and label-correction-hashes.json cover all eight files; pre-label-correction-hashes.json retains the prior boundary. A targeted mounted assertion checks both unique visible option labels, selects second-provider then third-provider, confirms each exact saved ID and distinguishing durable summary, preserves the draft, and verifies exactly two saves/no submit. TypeScript and all11 selected mounted checks pass at the default desktop viewport. Manually observed AX options Pin to: Mock connection (second-provider) and Pin to: Mock connection (third-provider), and the saved team summary includes its lead ID. Prior320/390 layout evidence predates this label-only delta; no new mobile matrix claimed. No controller expansion, native/provider/server/publication actions. Ready for independent label delta recheck.

## Unconfirmed-applied summary P2 correction

Read COMPACT_COMPOSER_STATE_REVIEW.md and its captured reviewerComposer.test.mjs apply-then-lost-reply characterization. Added a controller-owned, per-conversation confirmation record containing the last confirmed summary and acknowledgement state; the private record retains the exact original request. The host snapshot remains globally truthful and refreshable. Only compact composer presentation is held at its last confirmed summary while confirmation remains unresolved, with an explicit visible uncertainty status. Configuration edits and Send are fenced during confirmation, including keyboard Send. Existing recovery controls remain available after a normal refresh clears a general error.

A snapshot match alone never clears an unacknowledged mutation. Wrong/malformed or lost acknowledgements retain the original pending save. A matching durable receipt moves the presentation record to awaiting refreshed state; only a matching saved revision/draft/attachments/selection/team releases it. If refresh fails after acknowledgement, Retry draft save performs refresh only and does not create or replay a save. Matching explicit rejection clears the presentation fence; pending state is not inferred from arbitrary snapshot observations. Confirmed recovery resets stale editor uncertainty wording. Draft text is preserved throughout.

Four focused controller tests pass, including the new three-outcome regression. It verifies apply then lost reply and apply then malformed acknowledgement, explicit normal refresh with the actual changed host snapshot still published, unchanged confirmed summary, no replacement save or Send, exact original mutation replay, and transition after acknowledgement plus matching refresh. The acknowledged-refresh-failure branch verifies a single save total and a refresh-only recovery. Existing atomic pair, route/cardinality/staleness and uncertain-send tests remain passing.

Twelve selected mounted checks pass at default desktop, and at390x844 after adding the final stale-notice assertion. The new mounted case independently injects all three outcomes before mounting the adapter, checks the visible old summary and uncertainty after normal refresh, disabled Send, identical replay payloads, correct new summary after recovery, preserved text, and no submission. The last narrow run also verifies stale uncertainty wording clears. TypeScript passes. No full unrelated suite or native/provider check was rerun.

source-hashes.json and confirmation-fix-hashes.json contain the current exact eight-file boundary; pre-confirmation-fix-hashes.json preserves the label-fix boundary. This correction changes workspaceController.ts, HostWorkspace.tsx, ComposerExecutionControl.tsx, hostController.test.mjs and hostRenderer.test.tsx. Duplicate-label helper, CSS, scenario selection, galaxy/sidebar and accepted result/list source remain unchanged. Local renderer/controller evidence only; no native persistence/restart, provider, new server or release operation. Ready for independent focused state recheck.

## Acknowledged supersession P2 correction

Read COMPACT_COMPOSER_CONFIRMATION_REVIEW.md and COMPOSER_CONFIRMATION_UI_CLOSURE_20260910.md. The prior unacknowledged-summary and duplicate-label closures remain preserved. Added explicit acceptSupersedingConfiguration to resolve an already acknowledged save superseded by a newer legitimate host revision. It requires an acknowledged original record with no pending mutation, refreshes the host, validates the exact reviewed snapshot/catalog key, and requires a revision newer than the acknowledged request revision. It then rebases the local draft base to that reviewed revision and clears only the resolved presentation record. Local message text remains untouched and dirty; no host save or submission occurs. Unacknowledged observations remain ineligible and acknowledged writes are never replayed by this action.

HostWorkspace offers Review saved configuration while the record is acknowledged. It captures and displays the saved revision, route/team summary and saved message as a stable review. Accept saved configuration and keep my text passes that captured key. A second change during review rejects and requires another explicit review; the button never silently takes the latest render's revision. Successful acceptance returns focus to Message. Existing draft review remains separate.

Five focused compact-composer controller tests pass. The added case covers immediate and recovered acknowledgement supersession, refusal to accept an unacknowledged observation, identical recovery replay only before acknowledgement, a second external revision while review is open, preserved newer local text, zero writes on acceptance, and the next explicit Save draft using the accepted revision. Thirteen selected mounted checks pass: desktop before final focus/defaultPrevented assertions, then390x844 including them. The mounted supersession case tests both acknowledgement paths and explicit stale/fresh reviews. The existing uncertainty case now dispatches both Meta+Enter and Ctrl+Enter while fenced, verifies preventDefault, zero Submit, and unchanged draft. TypeScript passes.

Exact current eight-file boundary: source-hashes.json and supersession-fix-hashes.json. pre-supersession-fix-hashes.json retains the preceding confirmation boundary. Only workspaceController.ts, HostWorkspace.tsx, hostController.test.mjs and hostRenderer.test.tsx changed in this correction. No styling, compact-list/result, duplicate-label helper or editor component changes. Existing4317 synthetic fixture only. No providers, native operations, new servers, export or publication. Ready for independent acknowledged-supersession closure.
