# S04 cleanup acknowledgement bound — frozen successor

Ready for read-only recheck: `3325e095ad28ae2aceb208e5520109ebee60632b`, parent `1d789e5eab04eaa4d5157a4b7ef405e232f4cfe5`, Git tree `109a7e582422bc1565d621a7a7d72ec3f1b7bb5c`. 112 staged files, SHA-256 `091503beaa93caefc11436f4235a42017b469023132b7d9db9c205f0e67b7ace`. Four-file delta listed in tools/symphony/source-stage/GIT_IMPORT_CANDIDATE.json. Candidate clean, blobs match manifest, no remotes or root-index use. Separate visual edits remain excluded.

Reviewed local S04_DISCOVERY_REVIEW.md for frozen 1a33f47. This successor incorporates the prior identity fencing and adds bounded cleanup acknowledgement and truthful frontend cancellation reporting. Earlier receipts are historical.

Cleanup runs in one owned worker; the command waits up to two seconds for confirmation. Timeout or lost channel returns DISCOVERY_CLEANUP_PENDING with no models and quarantines further discovery until restart. The worker keeps ownership and may remain blocked in OS wait/removal. It holds no host operation or discovery lease. Quarantine prevents new discovery from accumulating cleanup workers. This bounds the command's wait, not OS termination; spawn, thread creation, panic fallback and spawn-failure scratch removal are not hard-deadline guarantees. Pending cleanup does not mean the child is confirmed gone. No paths, process output or OS error text escape.

Successful cleanup preserves session result, except cancellation observed during cleanup returns cancelled. Cleanup failure/pending takes precedence over success or cancellation and always keeps selectionEnabled=false. Frontend abort requests cancellation and waits for the native final outcome. False/true cancellation acknowledgement alone cannot resolve cancelled. Late success after abort becomes DISCOVERY_CANCELLATION_UNCONFIRMED; the ten-second frontend deadline is DISCOVERY_TERMINATION_UNCONFIRMED with timeout state, never confirmed termination.

Validation: 12 native discovery fixtures passed, including deterministic stalled cleanup-channel deadline/quarantine, pre-registration fencing, real synthetic child cleanup success/output failure/deletion failure, and cancellation injected during cleanup. Fourteen exact staged frontend discovery/default-capability checks passed, including false acknowledgement, late success after abort, cleanup precedence and mocked deadline. TypeScript checking and nine stage-validator checks passed. Static stage comparison covers 112 files. All process fixtures are synthetic; injected scratch retention was removed by the test.

Local logs: qa-artifacts/s04-codex-discovery-20260909/native-cleanup-bound-final.log, staged-frontend-cleanup-bound.log and stage-cleanup-bound.log. No private QA forwarded. No real account/model query, inference, paid call, auth mutation, native app launch/install, service/publication change or held issue retry. Next: independent frozen successor review, with OS-level isolation/termination and real compatibility still unverified.
