# S02 guarded provider configuration contract

Frozen successor `e49efc20e0c50609730787534ecfaa4556198b5a`, parent `d7e20910a39aab51acbe79460caac5e2d7e3f77f`, Git tree `ec8bd84c7058a9b0115fb0c230f77f17dfd92a2d`. Manifest 117 files; SHA-256 `ce52b84faf6f51d0c8b0411dc7235441cb5e87559ac7594d930800cada452f31`. Five-file delta in tools/symphony/source-stage/GIT_IMPORT_CANDIDATE.json. Candidate clean, committed blobs match manifest, no remotes/root-index use. Separate visual export preserved.

Implemented optional typed adapter for an explicit selected host route. Existing configure_provider accepts an optional schema-v1 expected-provider/selected-ID guard and returns an exact durable receipt after commit. Legacy unguarded callers retain null success. Guarded requests recheck installed route kind/path/canonical discovery ID against local filesystem search, under host mutation/workspace locks. This launches no provider process. Stale route/provider/selection fails before mutation; no auth/model probes or inference. The source adapter is available for future UI use, not auto-mounted.

Frontend accepts only matching provider fields and selected ID, otherwise returns sanitized uncertain without replay or draft mutation. No mutation-ID reconciliation or adapter timeout is included; future UI must inspect a fresh snapshot after uncertainty. Route identity binds canonical path, not binary content; same-path replacement and post-check filesystem races remain outside this guarantee. Global provider selection does not rewrite pinned conversation models/teams. Legacy route-ID migration remains explicit future work.

Validation: 2 native focused tests passed (guard rejection plus save-fault/no-receipt, successful durable acknowledgement, draft preservation and persisted reopen); 3 exact staged frontend tests passed (guard/acknowledgement, rejection/lost reply/mismatch, missing optional bridge and contradictory identity); TypeScript and command-registration --no-run compile passed; 9 stage checks passed. First persistence fixture failed on reopening a nonexistent synthetic executable; corrected to an inert local file, never executed. Native compile retains a pre-existing unused-variable warning in constellation_projection.rs, outside this slice. Logs: qa-artifacts/s02-provider-configuration-20260910. No actual provider route was configured.

Next-ready: independent contract review, then explicit route picker in existing Settings with separate user-triggered local route inspection and uncertain-save reconciliation before another attempt. Conversation binding must be visible; no automatic credential flow, model discovery, test prompt or send. S02 first live useful conversation remains unfinished. No native app launch/install, paid call, account/model query or publication occurred.
