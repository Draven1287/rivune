# Quiet Conversation: next visual acceptance

Prepared 2026-09-10 for the sole app builder's Claude refinement. Documentation only; no source changes or computer control. These are proposed measurable design targets, not user visual approval or new host capabilities.

## Evidence and scope

Read `prototypes/ai-native-workspace/design-qa.md` and `qa-artifacts/shadcn-quiet-preview-20260910/ui-review/REVIEW.md`. Reinspected saved `preview-desktop.png` and `preview-320.png` against the original 1487×1058 Quiet reference at `/Users/Aaravshah/.codex/generated_images/01a0847d-ff64-7d21-9285-20c144268a56/exec-7cb96637-eda1-4493-a4e7-748eb6dc2353.png`. These are saved snapshots, not a fresh running-source or native review. The 390 observations below come from the prior review. Later confirmation correction has functional closure in `ui-closure/REVIEW.md`; its transient pending toolbar still needs retained-state visual inspection.

Preserve Tauri, chat-first hierarchy, selected silver-orbit identity, peripheral galaxy and truthful Constellation provenance. Builder owns production source and Claude computer control. Reference research/review completion labels and filename are illustrative and cannot be copied as factual product state.

## Prioritized refinement

| Priority | Observed issue | Proposed acceptance |
| --- | --- | --- |
| P1 | Opening recommendation has body weight; result and metadata compete. | For an explicitly supported display title, use 24–28px/1.25–1.35, weight 600 on desktop; 20–24px on narrow. Body 16–18px/1.5–1.65. Title visually distinct within the first answer viewport. Do not guess a title from arbitrary saved text or mutate saved/copy bytes. If no safe title contract exists, improve role/body grouping and record that limitation. |
| P1 | Wide desktop prompt and flat answer feel sparse despite generous whitespace. | Keep body measure 640–760px and composer 720–820px at 1487. Align answer text, provenance and result action to one leading edge; composer inset difference no more than 24px. Compare equal-sized screenshots before selecting horizontal placement; do not treat the older report's direction-of-offset statement as a measured coordinate. |
| P1 | Narrow header/navigation uses substantial height; composer overlays the continuing transcript in the saved 320 view. | Keep all current destinations accessible. At 320×844 and 390×844, initial composer height at most 180px; permit internal draft growth/scroll without pushing Send offscreen. Scroll the final answer line and evidence action completely above the composer with at least 16px clearance. No document horizontal overflow. |
| P2 | Large paragraph gaps weaken answer grouping; status, inspect action and evidence feel detached. | Use 16–24px paragraph separation, 24–32px between message groups, 8–12px between role/status and answer. Group saved-result inspection and provenance with 12–16px spacing. Preserve exact inert text rather than silently normalizing its bytes. |
| P2 | Small subdued control text competes with empty desktop space. | Controls 13–14px minimum; body remains stronger than metadata. Narrow touch targets at least 44px high where practicable, with no overlap. Focus rings visibly distinguish active control against both dark panels and galaxy. Verify 4.5:1 normal-text contrast and 3:1 control/focus boundaries. |
| P2 | Packaged icon differs from selected silver-orbit reference. | Use only the selected, prepared asset. Inspect at 16, 22 and 32px for recognizable R/orbit and unclipped edges; keep current asset explicitly provisional until that asset exists. No guessed replacement. |

Keep central reading/composer surfaces opaque enough that stars never cross glyphs. Sidebar/right-edge galaxy may remain visible. Do not add avatars, contribution cards or additional settings simply to fill space; every factual state must have authoritative host evidence.

## Desktop acceptance checklist

- Capture the same synthetic conversation with Results closed at 1487×1058 and 1280×900, 1× density; compare reference and refinement without rescaling. Record actual font sizes, reading width, composer height and alignment offsets.
- Inspect Results open at 1201px: chat width remains at least 480px. At 1200px, use the existing replacement behavior, preserving mounted draft state and inert hidden Chat.
- Inspect long title, long provider ID and 2–6 participant configuration. No clipped title/actions or status disguised as confirmed selection.
- Open mode menu with keyboard, choose configuration, verify Execution mode receives focus, Escape restores its trigger. Settings Escape restores its opener.
- Open/select/close Results; preserve exact local draft, result identity and focus. No automatic artifact selection, read, save or send from decorative navigation.

## Narrow acceptance checklist

- Capture 390×844 and 320×844 with ordinary answer, end-of-answer, configuration editor and retained pending-confirmation state. Record scrollWidth <= clientWidth and actual visual inspection; rectangle existence alone is insufficient.
- At 320, long title may wrap but Results/Refresh and navigation remain reachable. Composer buttons and confirmed provider/team summary wrap without covering text or each other.
- Configuration editor fits between viewport edges, scrolls internally, and exposes lead selection plus final actions via keyboard and scrolling. Escape restores the correct opener.
- Hold lost/malformed acknowledgement and acknowledged-refresh-failure fixtures mounted. The visible toolbar must show the prior confirmed configuration, with no newer selectable labels; Send and keyboard submission remain fenced. Check that wrapping summary is unobscured.
- At 200% browser zoom on desktop, verify reflow, navigation, editor and composer access; record this as newly tested rather than inheriting prior acceptance. Native keyboard/IME and real screen-reader behavior remain separate unless actually exercised.

## Acceptance handoff

Return paired desktop images and narrow state captures locally, plus a short table of target met / deliberate deviation / unresolved. Run focused existing draft/configuration/Results interaction checks after styling changes; preserve authoritative send admission, newer/ABA draft edits, exact artifact provenance and confirmation fencing. Do not rerun unrelated suites without a change or failure that justifies them.

A functional pass permits independent visual review; it does not establish user visual approval, native startup/provider/clipboard proof, or release readiness. Claude's output is a refinement candidate until implemented and inspected against these states.
