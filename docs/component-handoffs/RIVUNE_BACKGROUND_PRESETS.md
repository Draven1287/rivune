# Background presets handoff

September 6, 2026. Source changes only in `Theme.swift` background-related definitions and SettingsView's Appearance section/storage properties. The existing Theme tail, including menu/focus primitives, is byte-identical to the start of this task. No changes to app routing, reading-surface palette, provider behavior or sidebar. No app launch/install, UI control, external assets or real UserDefaults writes.

Graphite draws the existing dark canvas quietly; Orbit adds static procedural arcs; Cosmos uses `RivuneWorkspaceCosmos`. All are still, so no Reduce Motion exception or background animation is introduced. Existing collaboration motion setting remains separate. Each custom selectable thumbnail uses the same renderer as the workspace, with selected/focused outlines and accessible names. The adaptive grid has 120-point minimum cards and collapses to fewer columns. Appearance toggles use `RivuneCheckmarkStyle`; brightness has a Rivune-drawn pointer/keyboard/accessible adjustable control; action buttons use Rivune styling.

Persistence: `rivune.appearance.backgroundPreset` recognizes only graphite/orbit/cosmos. Missing, empty or unknown values retain the exact previous galaxy/stars/dim composition. Selecting a preset writes only its new key. Optional `rivune.appearance.presetDim` and `rivune.appearance.presetStars` preserve new customization independently; absent values inherit existing brightness/star choices without writing them. Selecting/customizing/restoring a preset does not overwrite the old galaxy/stars/dim settings. “Use previous appearance” clears only the selected preset string; prior controls remain available in that mode. Preset rendering bounds darkness to 0.30–0.85 without rewriting stored values. The previous appearance path does not clamp or otherwise migrate existing values.

Validation: **77 pure policy assertions passed**, including all legacy boolean combinations across absent/invalid keys and brightness values, optional overrides, restoration, finite new-preset fallback and stable IDs. Test script compiles the marked pure production policy and uses no defaults store or app. Full Theme plus representative menu callers and the isolated Appearance section passed Swift 6/macOS 26 typechecking. This is not a full application build or rendered UI acceptance. Exact source/block hashes are in `qa-artifacts/appearance-presets-20260906/source-receipt.json`.

Native owner should integrate and inspect all three thumbnails/backgrounds at narrow/wide Settings widths, contrast behind real content, keyboard/VoiceOver selection and brightness, parent disabled behavior, persistence across relaunch, and previous-appearance restoration after preset customization. Also check the existing menu focus regressions after integration. Reading surfaces and palette tokens were not modified, but their combined visual result still requires rendered review. The owner may add “graphite orbit cosmos background” to the Appearance search terms while making its separately owned Settings routing changes; enum mappings were intentionally untouched here.
