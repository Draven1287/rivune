# Source coverage and activation state

September 9, 2026. Product-owner conversation and original complete pasted request were read. Recent turns were requested for 13 Rivune implementation/review/coordinator tasks plus Automate Codex chat task dispatch. Some provider task reads returned empty latest turns or older interrupted histories; these were not treated as current results. This is a synthesis of available task context, not a claim to have exhaustively read every historical turn.

Reconciled against docs/RIVUNE_DESIGN_DIRECTION.md, TAURI_PRODUCT_EXPERIENCE_20260907.md, CONSTELLATION_PRODUCT_REVIEW.md, NEXT_APP_DELIVERY_MANIFEST.md, current retry/source/state/visual/Dock receipts and LIVE_DISPATCH_STATE.md. The latest user direction overrides obsolete SwiftUI, Traycer and multi-writer instructions. Private transcripts, local profiles and raw diagnostic records were not copied to GitHub.

The public product brief is approximately four pages at 1,977 words and is embedded directly in WORKFLOW.md, so workers do not depend on a missing remote brief file. Fifteen bounded backlog topics have explicit scope and acceptance; existing website/download issue 4 is reused.

## Not activated

The official executable checksum was verified. No Symphony service has started. Automatic approval review rejected the prior persistent-service startup; do not retry or indirectly start it without explicit startup approval. The blank required label in current WORKFLOW.md matches no issues. Published backlog labels do not enable dispatch.

Remote main inspection did not find prototypes/ai-native-workspace or a Cargo.toml. S00 source reconciliation must precede app implementation. Publishing a product brief does not publish the local app or make local build receipts available to isolated workers.

## Activation sequence

1. Obtain explicit approval for the previously blocked service startup scope.
2. Reconcile source and active desktop ownership; finish the current app-builder slice first.
3. Choose a bounded ready issue and ensure prerequisites are actually met. Do not label the master goal for worker dispatch.
4. Replace the no-match filter with symphony-ready only when enabling workers; use one implementation slot initially.
5. Supply GitHub credentials via host environment/keyring, never workflow literal text. Start one process with a loopback dashboard and record its identity.
6. Verify tracker polling, one controlled issue admission, completion evidence and next ready-issue pickup. Only then claim automatic execution works.
7. On completion, retain evidence and move work to independent review. Coordinator verifies acceptance before releasing dependent tasks. Backlog prose is not a built-in dependency engine.

Symphony creates issue workers, not the existing sidebar task sessions. Its ready-issue scheduling can refill capacity while service, network and host remain available. Do not promise it runs through sleep, shutdown or authentication failure. Keep the existing coordinator until the handoff is verified; never let both assign the same implementation.
