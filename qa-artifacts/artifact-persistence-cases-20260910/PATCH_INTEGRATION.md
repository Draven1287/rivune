# P03/P05/P06 native test patch

Status: UNCOMPILED AND UNEXECUTED Rust tests. No test profiles have been created. No production edits or native build occurred.

Patch: p03-p05-p06.patch, generated against source-snapshot/saved_artifact_tests.rs. Review manifest.json for the captured baseline hashes. additional_tests.rs contains the same append-only addition for manual integration if the builder's working module has moved. Append exactly once inside the existing host::saved_artifact_tests module; do not create a separate integration test crate. Existing use super::* and imports supply HostState, PersistFault, WorkspaceSnapshot, RunRecord, RunActivity, PersistedConstellation, RunEvent, TeamSession and fs. Existing profile/run/council/complete/success/query helpers are reused unchanged. std::path::Path is fully qualified; no new dependency/import is needed.

Builder should check patch applicability against its frozen artifact source, then apply only to its owned saved_artifact_tests.rs. If helper signatures have changed, adapt there rather than editing the captured audit baseline. Use its existing single native Cargo target/cache. Suggested filter for its normal library test invocation: saved_artifact_p (three tests, three fault values each; nine cases). Preserve exact compiled source hash and output in the builder receipt. Passing is not claimed until that run completes.

P03 injects exactly one terminal save fault. P05 and P06 inject exactly two checkpoint faults because save_constellation_checkpoint automatically retries once. Each case reads the latest .json snapshot before reopen and performs no reconcile or successful save after the injected failure. .pending files are excluded. P03 asserts answer/artifact atomicity; P05 asserts checkpoint/member-artifact atomicity and existing member retention; P06 asserts terminal run/checkpoint/final-artifact atomicity and existing member retention. Open may repair a committed running run to failed: assertions explicitly allow that expected recovery, rather than claiming cancellation or completion after a pre-rename failure. Reopened artifacts must equal the exact committed vector, not merely its count.

All test execution is synthetic helper-driven persistence only. No provider adapters, executables, app launch, N5 shutdown, real profile or installed state is accessed. Cleanup in the tests targets only fresh paths returned by the existing synthetic profile() helper.
