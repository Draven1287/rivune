# Startup fallback independent UI review — 2026-09-10

Bounded PASS:12/12 selected actual React startup-fallback cases independently observed at verified innerWidth1280,390,320. All eight current source/test hashes match the implementation manifest; exact expected/actual hashes in hashes.json. No actionable visual defect found. This is containment acceptance, NOT an S02 repair or native boot proof.

Read implementation receipt, StartupFallback/StartupLoader and actual selected assertions. Ran only existing4317/tests/hostRenderer.html?scenario=startup-fallback. The cases exercise throwing child, rejected/pending module, valid desktop, intentional demo, marked missing/null/primitive/accessor bridge, partial bridge, hydration-error and hydration-uncertain. Fallback assertions check appropriate role status/alert, no controls, no private error strings, zero accessor calls and no host/storage mutation. Valid/partial/hydration paths preserve existing host validation/state handling. Intentional demo storage behavior is compared to direct-App baseline; it is not claimed mutation-free.

Used supported fallbackPreview to inspect failed at320×844, bridge-unavailable at390×844 and loading at1280×900. Screenshots viewed locally only. Text wraps legibly on calm dark surfaces with clear headings, without horizontal overflow. Loading has aria-busy=true, role=status text Waiting for the workspace interface, and zero controls. Failure has role=alert and says latest save status cannot be determined without asserting data loss. Missing bridge has a distinct heading/explanation. No private error, retry/reload/reset/save/send action appears. Viewport reset afterward.

Compared to proposal: same static loading root, quiet stylesheet, error boundary, desktop-marker guard and honest wording. Builder moved dynamic import into StartupLoader with an optional controlled loader seam and effect disposal guard; tests now exercise the real shared loader instead of a duplicate promise path. No timeout or global exception handler added. Caught hydration failures remain existing HostWorkspace state, correctly not relabelled render failures.

Limits: role/semantic DOM and visible layout were verified, not actual screen-reader speech. A busy ancestor may affect live-region announcement timing across assistive technologies; no spoken announcement claim. The12-case fixture is runtime synthetic evidence, not native disk, WebKit CSP, initial HTML delivery or real module-network-failure evidence. Static-root survival when entry/bootstrap fails was source-inspected, not tested by disabling scripts. Pending/disposed loader completion coverage is owned by lifecycle reviewer; no exhaustive timing claim here. Native HTML-not-loaded remains outside frontend containment. No native operation, provider/account call, new server, source edit or private artifact forwarding.

Next: combine UI verdict with entry/build/lifecycle review before sole-builder acceptance. Actual S02 diagnosis and safe-retirement authorization remain separate.
