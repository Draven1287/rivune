# First shared browser preview review

Reviewed http://127.0.0.1:4317/ in an independent in-app browser tab using the sole existing server. No implementation edits, new server, desktop launch, Cargo or build execution. Source remained live during review; OBSERVED_SOURCE_HASHES.json records end-of-review bytes, not an immutable test baseline.

P2 sent frontend and coordinator: at320x568, Conversation with telemetry expanded leaves conversation-scroll24px high (composer169.89px, shelf-content140px). Screenshot evidence is in the reviewer conversation. The transcript cannot present a readable message; collapsing telemetry restores it. Cap/collapse auxiliary content at short heights while maintaining a usable transcript minimum. Verify both resize from expanded desktop and opening telemetry at this size.

Independently rendered checks: wide1440x900 sidebar keyboard224→236 and split47→49 persist after reload; pointer sidebar236→257 works. At1100 auxiliary editor switches to a tab;390x844 composer remains usable. Slash Down+Enter selects build, Escape retains draft, unmatched Enter does not submit. Nonempty draft disables Commands to preserve it. Diff has signs and accessible Added/Removed/Unchanged labels. Invalid preview JSON displays retained-source/nonexecution error and restoring source restores authored canvas. Stop demo produces local stopped text. Scrolling to earlier messages during local streaming retains scrollTop0 through completion with Latest message action. Emulated reduced motion gives stream cursor/skeleton animation0s and transition0s; emulation cleared afterward. No captured console errors. Telemetry/demo labels explicitly avoid real usage/private reasoning claims.

Screenshots inspected at desktop,390x844,320x568 expanded/collapsed shelf. Browser viewport reset afterward. IME composition was source-inspected only, not generated through a real input method. No screen-reader session/full WCAG contrast audit, native webview acceptance or host integration claim. Owner typecheck results are separate evidence. Live hot reload means fixes need a stable follow-up checkpoint before final acceptance.
