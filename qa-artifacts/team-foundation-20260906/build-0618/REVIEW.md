# Native Rivune Team review build 2026090618

Installed and opened `/Applications/Rivune.app`, version 0.2 build 2026090618. Local ad-hoc signature passes strict deep verification. This is a local review installation, not a signed/notarized public release or App Store submission.

## Delivered

- Native Team editor beside the composer. Two to six independently identified members can share a CLI. Members keep requested model and reasoning independently. Orchestrator selection appears first; optional ordered fallback is secondary. Save persists a separate team preset; Cancel discards the edit draft.
- Native Council submissions freeze the saved team before publication. Retry uses the old snapshot and current admission evidence, not the current composer. Orchestrator drafts independently before separate review. Same-CLI members are distinct work, scheduled within conservative shared Council limits (two simultaneous calls total, one per route), including synthesis, fallback and word-budget repair.
- Codex model/effort combinations come from the installed CLI cache. Claude help exposes a model list and global effort list, not an exact model-effort matrix, so Team admission currently supports advertised Claude models with Automatic reasoning only. Account defaults stay unresolved. Unsupported, stale or unavailable choices fail closed. Metadata timestamps retain original observation times; getters do not renew them.
- Native product dropdowns in the composer, models, conversation/project actions, inspectors, Settings and API connection UI use Rivune-designed controls. Actual Mac window controls and system file/security/share dialogs remain native platform UI. Transients dismiss on app deactivation/hide, other-app activation and owner-window focus lifecycle.
- Malformed saved Council journals are preserved before startup normalization or retry publication. Cardinality, exact result identity and legacy transport identity checks precede mutation. Valid old records retain their legacy behavior and IDs.

## Validation

- 276 native tests passed, zero failed/skipped; `test-summary.json` records the exported xcresult. Final universal Release build: `/private/tmp/rivune-0618-final4-build.log`.
- Independent checkpoint 4 review closes the reproduced journal defects: 12 coordinator scenarios, including ten invalid journal cases, plus carried-forward 15 unchanged runner scenario groups. Adapter reviewer separately passed 19 focused freshness/admission checks. These are synthetic/source checks, not live account entitlement or model-quality proof.
- Native isolated UI: Team opens as an owner-attached sheet; orchestrator is immediately visible; Down/Return selects and restores focus; Escape dismisses menus. Saved two-member and three-member same-CLI configurations reopen/return with correct Team count and selected orchestrator. Advanced fallback choices and scrolling were exercised. No provider requests were submitted.
- Right-click conversation actions expose custom grouped buttons, including selection and destructive action labels; Escape closes the popover. No destructive action was selected.
- Actual Finder activation (opening a Finder window) dismissed both attachment and Team dropdowns. Returning did not reopen them. Cmd-H also dismissed menus. CUA AX-only clicks/raise and Cmd-Tab attempts did not establish application activation and are not counted as standalone Cmd-Tab acceptance. Temporary Finder windows were closed.
- One earlier staged QA process unexpectedly disappeared and was relaunched by CUA without launch arguments; no contemporary crash report was found. No crash/root cause is claimed. An explicit `RivuneUIPreview` Info.plist clone was then used so any automation relaunch remained isolated; clean Save flows passed twice without recurrence. The installed candidate has no preview flag.
- Installed normal startup restored all 24 existing conversations and reported both CLI connections ready. All four real history/project/draft/journal JSON files are semantically unchanged versus pre-install backup. `history-verification.json` records this. Readiness is not a new paid/live provider evaluation.
- User interaction was detected in the installed app after launch; native UI ownership was released to root/user and further UI actions stopped.

## Identity and rollback

Executable SHA256: `885efabc70eef4c63b96d2add18c0414c98cc6acf0c52b742874fe83b621de94`.

`source-manifest.json` records the 44 mirrored Swift/project/configuration source files used for the build, including StartupReadiness.swift. Workspace source hashes matched the build mirror at installation. Full source copy: `/private/tmp/rivune-review-2026090618/source`.

Rollback: `/private/tmp/rivune-review-2026090618/Rivune-replaced-0617.app` (an additional pre-install copy also exists). History backup: `/private/tmp/rivune-review-2026090618/history-before`.

## Limits and next milestones

Swarm and Auto remain unavailable in this build and are required later milestones. Arbitrary discovered CLIs require registered adapters; Team execution currently admits Codex CLI and Claude Code CLI only. API conversations retain their separate existing connection flow. No new live evaluation, provider-quality superiority, account sign-in activation, public download release or cloud execution is claimed. Existing 0/3 Council grounding evaluation result remains unchanged.
