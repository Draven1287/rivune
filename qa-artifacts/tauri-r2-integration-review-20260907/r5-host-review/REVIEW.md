# Frozen R5 host independent review

P2 correction required: team uniqueness differs between host and renderer. All three frozen source hashes and actual fixture JSON ffa9a563 verified; generator2b977d is a separate source hash. No competing Rust/build/native/provider execution.

Host validate_team_structure inserts the whole ModelSelection into HashSet. Derived Hash/Eq includes catalog_revision. Therefore two members with the same provider/model/effort but catalog revisions A and B pass host uniqueness when the selected lead matches member0. save_rich_draft_in and reopen use this validator, so this can become a durable team. Renderer parseTeamSelection intentionally keys uniqueness by providerID/modelID/effortID, rejecting that same value as Duplicate team route; parseSnapshot then cannot load it. Independently executed that renderer rejection against the frozen core. Host acceptance is source-derived, not an independently executed Rust result.

Use the shared route tuple for host uniqueness, retaining full selection identity for mutation/CAS/lead comparisons. Add actual save/reopen rejection for same route/different revision, plus distinct-route acceptance and a real serialized team fixture consumed by renderer. Do not solve by weakening renderer validation or claiming engine connectivity.

Other inspected scope: selection/team comparisons participate in immutable mutation replay, atomic candidate save, durable receipt and reopen structural checks. Resync uncertainty correction is retained. Admission checks rich revision/prompt whenever choices or attachments exist, binds exact current catalog, clears legacy provider.model for explicit provider-default selection and rejects unknown model/effort. RuntimeCapabilities and constellation admission remain ENGINE_NOT_CONNECTED. Catalog performs local file metadata checks only and preserves authentication unknown/response notTested; default-only capability is not explicit-model/provider-response proof.

Frozen actual JSON catalog and requested selection parsed successfully using frozen frontend core. This is a partial wire check, not full runtime/engine acceptance. Owner88library/2binary tests remain owner-reported. Team duplicate mismatch blocks qualified host/renderer integration until corrected. Native/provider/Council execution and subsequent moving source remain outside this checkpoint.
