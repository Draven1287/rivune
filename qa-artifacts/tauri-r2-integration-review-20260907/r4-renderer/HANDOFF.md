# R4 integrated renderer handoff — review pending

Canonical renderer now implements Attach -> exact text preview -> explicit Include -> durable rich draft; approved file chips offer inspection/removal. File selection and bytes remain host-owned. No renderer path or content input reaches the picker/approval commands. Preview is Rivune UI; native file selection remains a platform host responsibility.

All text writes now use saveRichDraft CAS and immutable mutation IDs with ordered attachment IDs. Uncertain receipts retain the exact pending request, and only durable matching receipts enable Send. Newer edits queue after reconciliation. Submit carries richDraftRevision and exact saved prompt bytes. Host source validation must pass before first attached submit; exact retry remains a host responsibility.

Shutdown preflights all draft saves before beginning. A persistent hostShutdownToken survives successful/uncertain begin and repeated Quit. Post-begin failure never unfreezes the renderer alone. Explicit Keep working invokes runtime-supported abortShutdown; only acknowledged host success clears the token/restores editing. Runtime must make successfully recovered token handling idempotent to recover a lost abort acknowledgement; that host detail was requested, not verified by renderer fixtures.

Exact normal IPC: save_rich_draft({conversationId,mutationId,expectedRevision,draft,attachmentIds}), response {state,mutationID,conversationID,revision,attachmentIDs,error?}. Rich shutdown uses clientRevision and expectedRichRevision separately, returning clientRevision and richDraftRevision; null conversation permits only empty text/IDs and null rich revision. inspect_text_attachment takes optional runId; get_snapshot must redact captured document text from both attachment arrays and approvedContext.documents. Runtime confirmed/owns that private-public boundary.

Renderer files: web/app.mjs, core.mjs, desktop-host.mjs, index.html, styles.css. chrome.mjs unchanged. Tests: app.test.mjs, attachments-bridge.test.mjs, attachments-browser.cjs, integrated-browser.cjs. No Rust/Cargo edits, build, app launch, install, provider execution or public action.

Current validation: 18 focused Node checks; 33 synthetic attachment browser checks covering explicit approval, no cross-chat leakage, restored snapshot, changed-source rejection, exact prompt/revision, uncertain save identities, removal, minimum-width rendering, selected/background conflict recovery, repeated Quit, uncertain begin and explicit abort. PASS3_HASHES.json binds reviewed source; broader regression suite is being reconciled with the new durable-only Send behavior. Screenshots are synthetic Chromium, not native acceptance.

Independent review P1 was reproduced and corrected; awaiting stable PASS3 recheck before final release. Native R3 pristine build/profile remain untouched. Final host integration must exercise actual secure file picker, persistence faults/restart/private-public DTOs, admission and shutdown semantics; fixture passes do not establish those results.

Final renderer disposition: independent PASS3_RECHECK.md closes original P1 at renderer scope. Current 67 integrated browser, 33 attachment browser and 18 focused Node checks pass. FINAL_HASHES.json is the release boundary; renderer/bridge/tests released to runtime for combined integration. A separate host P2 remains: applied abort with lost reply needs authoritative same-token idempotent recovery, including rejection of stale tokens against newer shutdowns. Runtime owns that fix and actual-host tests. No native/build readiness follows from renderer closure.

Restart acknowledgement correction (review pending): app d138aa3416d0c0f7531ae16069991c8e5a9de0f00b9318e986c0860af171e2d7. Immediate/persisted recovery now binds durable richDraftRevision and ordered attachmentIDs; fixed-CAS cleanup waits for durable receipt and rechecks local state before clearing. Legacy recovery lacking identity conservatively retains draft. RESTART_ACK_CHECKS.json records46 targeted browser checks, including newer changed/removed-file/text preservation and host CAS rejection; existing67 broader browser regressions and18 Node checks passed afterward. No shutdown-function changes. Await independent rereview before returning narrow app/test ownership.
