# Connection handoff independent mounted UI acceptance — 2026-09-10

Bounded PASS. No actionable UI defect found in this scope. Four implementation/test hashes independently match the receipt manifest; exact expected/actual hashes recorded in hashes.json. No live/candidate source changes.

Read CONNECTION_HANDOFF_IMPLEMENTATION_RECEIPT, current ProviderSetup lifecycle/handoff conditions and the selected mounted assertions. Independently observed all11 connection-handoff cases PASS at actual innerWidth1280,390 and320 using existing4317 only. Narrow selection remained connection-handoff; no Results or unrelated suite. The generic report scope says selected team forms only; the eleven named results identify the actual selected coverage.

## Executed evidence

Mounted cases cover explicit durable save, unconfirmed matching snapshot blocked, display refresh failure/recovery, applied/rejected/no-operation recovery, pinned/team bindings plus Settings reopen, late old-bridge configuration save, missing routes and absent conversation. Successful cases assert existing Message textarea/draft/focus, no submission, and single configuration; recovery checks no apply replay. The no-conversation case checks actual button absence, not notice wording.

These are actual React fake-host tests: DOM values and node identities, controlled responses and assertions, not real authentication or native durability. Readiness copy consistently leaves sign-in and successful response unverified. Workspace-default identity does not imply the current pinned/team conversation was rebound; mounted binding tests compare saved richDraft and assert no second configuration on reopen.

## Independent keyboard check

Used retained handoffKeyboard=1 fixture with fake durable configuration. At320, starting on Close Settings, Tab twice reached Continue to chat; focus scrolled the dialog so the action was visible, with a clear focus ring. Return closed Settings and focused Message with exact `Keyboard handoff draft`. Page scrollWidth320, no horizontal overflow. Screenshot verified readable wrapping and reachable Continue; fixture's bottom status overlay limits evaluation of bottom composer controls, not the Settings action.

At390 and1280, reopened Settings on same bridge, tabbed from Close through Find installed providers to Continue, then Return. Continue was visibly within viewport (390 top585/bottom627;1280 top705/bottom747). Each handoff closed dialog and restored Message with unchanged draft. No Copy/Save/Send/provider action was manually used. Viewport reset afterward.

Visible composer status: workspace default ID from snapshot; save did not test sign-in/provider response; conversation uses selection above. It is truthful, although deliberately technical provider IDs are exposed rather than guessed friendly readiness. No broader redesign requested.

## Lifecycle context and limits

Current implementation remounts a private setup session by bridge identity, uses layout-effect lifetime guards and reactive busy state, and captures parent controller refresh. The selected mounted old-bridge-save case passed; state reviewer independently owns source async-race coverage. Pending discovery/reconcile/refresh replacement permutations, StrictMode exhaustive timing, real host persistence, authentication, provider output and OS restart are NOT established by this UI review. Recovery acknowledgements and state are synthetic. No new server, native launch/build, account operation, dependency, production edit or private-artifact forwarding.

Next dependency: combine this bounded UI verdict with independent state review before sole-builder export/integration acceptance. No UI correction requested.
