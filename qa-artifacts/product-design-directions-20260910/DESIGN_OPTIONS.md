# Rivune visual selection — September 10, 2026

Three independent ImageGen results, numbered by their actual display order in RIVUNE APP BUILDER. Exact paths/hashes are in options.json. No live UI or accepted source changed. These are synthetic design concepts, not functional or native acceptance. Await Aarav selection before implementation.

1. Quiet Conversation — recommended default. Expanded sidebar, restrained inline participant summary, readable centered chat, results closed until requested. Lowest navigation friction and clearest reading priority.
2. Focus Rail — rail plus secondary navigator, participant information in margin. Strong separation but consumes more horizontal space than intended; generated image duplicates Settings. Those rough edges must be corrected in a selected target before implementation, not copied as requirements.
3. Conversation and Result — on-demand document pane open beside chat. Useful for reviewing an artifact without losing the conversation; reduces reading width and is best treated as an explicit Results-open state.

Shared synthetic content compares SQLite with JSON for a notes app. No screenshot/draft text was sent to generation. The two user screenshots were inspected locally only and show older S02, not current source proof. The actual silver/orbit brand image was attached to each generation. Output maintains the same sample prompt, answer, three participants and one saved artifact. Body/control scale is conceptual and needs exact token normalization once selected. Prompts targeted1440x1024; returned image dimensions are generator-controlled, not a pixel-exact implementation contract.

Architecture/behavior context: current app is React/TypeScript over Tauri/Rust with explicit bridge contracts, host-owned persistence/recovery, Constellation execution/projection and saved artifact inspection. Browser demo is an intentional separate simulated mode. Recent startup, draft, result and handoff checks include actual controller tests and synthetic-host mounted tests; accepted isolated builds are not proof of a real-provider native workflow. Startup fallback is accepted containment, not a demonstrated S02 root-cause fix.

Five relevant gaps: coherent visual hierarchy; compact composer with preserved save/draft safety; actionable Connections states with installation/sign-in/response test kept separate; comprehensible actual participant states and combined results; verified prompt-to-result native vertical slice. Existing source and verification records were reused rather than a new broad audit.

After selection: coordinator arranges bounded specialist checks; sole builder implements selected design incrementally in existing4317 preview. Cover new conversation, active chat, actual/synthetic-labelled Constellation progression and result, and Connections. Preserve existing backend, draft/history/recovery and result logic. Compact Connections rows reveal technical detail on demand and one clear next action without conflating provider states. No new dependency is selected or installed by this ideation; editable MIT shadcn components may be evaluated later where compatible, with existing dependencies preferred. No native app/build/export/launch/replacement, paid service, hosted service or publication.
