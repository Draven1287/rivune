# Quiet confirmed-toolbar correction — independent UI closure

Bounded PASS for mounted confirmation behavior. Refreshed13-file manifest independently matches current source, exact hashes in hashes.json. No new defect found. Prior visual-fidelity qualifications remain unchanged.

Read CONFIRMATION_CORRECTION.md and final new scenario implementation. Opened only existing4317 quiet-workspace selected harness. Independently observed5/5 named scenarios PASS at verified1280 and320 CSS widths; the fifth iterates lost reply, malformed reply and failed refresh after acknowledgement. Viewport reset. The existing selector groups the four prior cases with the correction, so they executed incidentally; no unrelated suite was run.

Actual mounted assertions compare visible .quiet-execution-toolbar text to the prior confirmed summary after applied-save snapshot changes, assert a rendered rectangle and absence of premature mode/provider buttons. Lost/malformed paths repeat that check after ordinary Refresh. Send remains disabled; both Meta/Ctrl+Enter are prevented with no submit or draft change. Recovery reuses byte-equivalent original save payload for unacknowledged cases; acknowledged refresh retry performs no second save. Final toolbar displays Constellation and Mock connection (second-provider) only after confirmation clears, with exact draft retained and stale editor notice removed.

Implementation replaces compact buttons with a wrapping confirmed-summary span while uncertain; it does not derive visible pending labels from the newer snapshot. Existing controller authority remains intact. These mounted DOM assertions prove actual rendered text, not only hidden summary/title. However getClientRects alone does not prove full unobstructed readability. The scenario unmounts before completion; this reviewer did not capture the transient uncertain toolbar screenshot or manually scroll it. Final report page width320 is not itself proof of intermediate toolbar overflow. The owner's320 correction evidence and wrapping CSS support layout intent, but manual pending-state pixel acceptance remains unverified. No claim that all intermediate text was independently visually inspected.

No source/server/native/provider/OS clipboard action, no private screenshot forwarding. This closes the confirmed-label functional regression in synthetic UI; native/provider behavior and final design approval remain separate. If full transient readability closure is required, expose a retained uncertain state using this same fixture rather than rerunning unchanged scenarios.
