# Central independent core-only integration review

ACCEPT the exact core-only source/build candidate for the existing authorized local integration/install workflow. No new blocking merge finding. This is not rendered/native smoothness, live-provider, distribution or post-install data acceptance.

## Exact identity and scope

Verified MANIFEST.sha256 identity cd465dfb8fd643fe7f76e8b5c1bee707987a02995aae990e296510450a90e729 and all12entries. Patch15128aeec8ff589e8aa35aec7b3b5d0be8b3cc945c6e99258115d3c7a807fe07 reconstructs all5candidatefiles exactly from accepted0623 in a disposable tree. All5files also match the actual owner build tree /private/tmp/rivune-core-only.V2F2UX.

Compared each candidate file byte-for-byte with the three prior accepted component packages: CouncilRunner and its test file; WorkspaceView and deterministic tests; TerminalAIService. No merge-only changes. The other73files under Rivune, RivuneTests and Rivune.xcodeproj match accepted0623, with equal file sets excluding xcuserdata. Verification recorded in central-evidence/verification.json. No phone5, Swarm, menu or provider-draft correction included.

Read the prior Council, transcript and terminal independent reviews and this complete patch. Validation changes concern saved lead identity/eligibility; transcript change routes content-update scrolling; terminal change suppresses SIGPIPE on the owned descriptor and reaps the directly owned process on post-launch error. The three changes affect separate execution/UI paths. Previously accepted targeted evidence remains applicable to these identical bytes; it was not redundantly rerun.

## Combined evidence

Hash-verified owner full-test log contains TEST SUCCEEDED, and its structured summary records326passed/0failed/0skipped/no runtime warnings. Hash-verified macOS Release and generic iOS Simulator Release logs both contain BUILD SUCCEEDED and identify the checked staging tree. These are independently verified owner build/test records, not central reruns. No need to repeat unchanged component suites solely to inflate test counts.

## Follow-through

Native owner may proceed through the already-authorized apply/build/install workflow with exact installed build/binary identity, immediate pre-install backup and post-install semantic draft/history preservation checks. Preserve accepted0623 rollback. Resolve any actual baseline drift before applying; do not silently add optional patches. Actual native scrolling, reader-away behavior, Settings/caret, close/reopen and resize checks remain unverified until a working authorized native UI path is demonstrated. Local review installation is not an assertion those checks passed.

Documentation correction: HANDOFF says transcript wheel routing, but the patch changes onChange-driven content-update scroll policy, not wheel-event dispatch. Use content-update scroll routing in later receipts. This naming correction is non-blocking and does not change the frozen candidate.
