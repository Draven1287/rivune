# Desktop correction closure

2026-09-10. Bounded source PASS; previous P1 closed. Actual main.rs hash `cd29d3fa5d7f5aa0bd467b47b35af3fa9e42dffaaa07b21e5a969657c339cd1d` matches correction manifest. Inspected correction receipt, current implementation and startup/Dock logs. No native compile, launch, process operation or source edits.

Preflight now executes after Builder::build supplies PathResolver but before app.run. Typed contention returns SUCCESS directly from main, without entering Ready/setup. Fatal resolution/build errors return FAILURE. Effective configured windows remain deferred before build. This correctly avoids pinned Tauri 2.11.5 app.rs:1422 setup-on-Ready panic boundary rather than attempting to catch it in build's error branch.

Successful preflight retains StartupWorkspace (and its Arc<HostState>/profile lock for healthy startup) in the private shared slot until Ready takes it. There is no successful-host drop/reopen gap. Setup manages startup/navigation/shutdown and optional HostState before UI construction. Genuine recovery has no HostState, but gets normal recovery status/gate and disabled Settings. The setup closure captures fallible initialization and passes it to complete_startup_setup; failure records exit status, calls app.handle().exit(1), and returns Ok to Tauri. Pinned AppHandle::exit requests runtime exit and falls back to cleanup/process exit if that request fails. This is not the old Err/panic path or an Ok-only headless loop.

The production RunEvent callback wraps the entire match in admitted_event. Admission remains false on setup failure, so ExitRequested is not prevented and no ordinary shutdown request or missing managed-state access is attempted. Reopen/Close/Ready/Exit are also excluded. Admission becomes true only after all initialization completes. Existing healthy/recovery Dock guard and shutdown handshake remain unchanged for admitted startup. If construction fails after creating main, partial UI may exist until controlled exit; zero UI is guaranteed by the inspected admission ordering for contention, not for every possible constructor failure.

Saved evidence reports eight startup tests and two Dock tests passing. New preflight test uses a fresh held profile and checks contention success/fatal resolution failure; new setup-completion test invokes the actual completion helper and dispatcher with injected exit/constructor behavior. They address the prior boundary gap at source/helper level. ExitRequested is not synthesized; all-event guard coverage is established by production wiring inspection, not OS delivery. No independent test rerun performed.

No remaining blocker identified in this narrow correction. Host typed-lock implementation remains under separate review. Actual process exit status, AppHandle exit delivery, absence of duplicate OS window/tray and double-invocation behavior remain unverified until separately authorized native smoke. No existing-owner activation or full single-instance IPC behavior is claimed. This closes P1 in source, not native runtime acceptance or permission to launch/package.
