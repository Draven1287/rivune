import {test} from 'node:test';import assert from 'node:assert/strict';import {visibleConversations} from '../src/host/conversationProjection.ts';
test('ties invalid dates latest-of-many and old active are deterministic without mutation',()=>{const rows=Object.freeze(Array.from({length:12},(_,i)=>Object.freeze({id:String(i),title:'Title '+i})));const runs=Object.freeze([{conversationID:'1',updatedAt:'2026-09-10T01:00:00Z'},{conversationID:'2',updatedAt:'2026-09-10T01:00:00Z'},{conversationID:'1',updatedAt:'2026-09-09T01:00:00Z'},{conversationID:'11',updatedAt:'invalid'}].map(Object.freeze));const result=visibleConversations(rows,runs,'0','',false);assert.deepEqual(result.map(r=>r.id),['2','1','11','10','9','8','7','0']);assert.equal(result[7],rows[0]);assert.deepEqual(visibleConversations(rows,runs,'0','TITLE 3',false),[rows[3]]);assert.equal(visibleConversations(rows,runs,'0','',true).length,12);});
