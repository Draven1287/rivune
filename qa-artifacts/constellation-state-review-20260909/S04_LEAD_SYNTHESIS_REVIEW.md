# Lead-synthesis acceptance — 2026-09-10

Disposition: **bounded acceptance; no actionable defect found** in frozen `7ff40f55b43fde56297a96b5806ee80bff3dbac9` against `016bde7243034e8059d266ca5ce6ff9f28483410`. Reviewed all four changed files and the lead-synthesis receipt. This accepts semantic correction and default evidence disclosure behavior, not visual approval, real AI performance or launch readiness.

- `src-tauri/src/team_strategy.rs`: the Council delivery produced after Role::Integrate now has reviewed=false. Scheduling still gathers independent answers and reserves one lead integration call. No separate Role::Review invocation, additional call budget or new execution stage was introduced. The corresponding test verifies Integrate and no next invocation. Swarm's implementation is outside the changed branch and byte-unchanged in the diff.
- `src-tauri/src/constellation_projection.rs`: Council PublicResolution always emits kind=leadSynthesis and reviewed=false, including legacy delivery flags set true. Projection accepts shared references to Session/contributions/delivery, builds new public values and does not mutate or save checkpoints. Non-Council delivery still yields no Council resolution. The existing structural fixture explicitly supplies a legacy true flag and now expects false. The legacy reviewerMemberID field remains for compatibility; visible copy identifies synthesis rather than inventing a review invocation.
- `prototypes/ai-native-workspace/src/host/Constellation.tsx`: resolution evidence says Lead synthesis and explicitly states that no separate peer review ran. Outer evidence and individual answers remain details elements without open attributes. `HostWorkspace.tsx` places the canonical answer in its own article before status/evidence; it was inspected from this frozen commit, not the mutable workspace. Resolution remains Council-only under the current public contract, so this copy does not alter generic Swarm semantics.

Independent verification: all **112/112** frozen Git blobs match source-stage manifest paths, byte sizes and SHA-256 values; the manifest identifies tree digest `ba23fa427d868358e0d6b9bb3d34e66bc2b04d1c2aeb5c031b7d86f231c7f7e7`. An own React static-render fixture loaded the exact frozen component and dependencies from Git, using installed React/TypeScript without a build or server. **1/1 passed**: honest wording even with a synthetic legacy reviewed=true input, both evidence levels initially closed, and no duplicate canonical answer inside evidence. Reproduce with `node --test qa-artifacts/constellation-state-review-20260909/lead-synthesis-frozen.test.mjs` from the workspace root. This verifies markup/default disclosure state, not browser layout or visual acceptance.

Retained native logs inspected: five Council checks and six projection checks passed, with overlap between those groups; they are not eleven unique tests. These executions remain owner evidence, not independent native reruns. The receipt additionally reports 31 mounted checks and two public fixtures; this review does not recast them as independently executed. The stale test name mentioning a copied review flag and unused projection closure variable are cosmetic, not acceptance blockers.

No discovery re-review, product source edits, providers, native launch/install, server changes, private evidence forwarding or publication occurred. Separate visual changes remain outside this candidate and acceptance.
