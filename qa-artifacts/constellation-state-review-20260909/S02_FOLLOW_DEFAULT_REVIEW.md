# S02 follow-default review — 2026-09-10

Frozen 6119dad12a2af59bee4f77e7d616e2cdfd76b46d versus 49777f343d8488621060d31edfd5666c418a9c1b. **Acceptance pending one P2 at inherited native admission.** Reviewed eight-file delta, receipt, existing native admission and focused frozen controller tests locally.

## P2 — Inherited sends bypass provider-default model resolution

`prototypes/ai-native-workspace/src/host/workspaceController.ts:145` now keeps null selection through send. The corresponding native fallback at `qa-artifacts/cross-platform-shell-20260907/candidate4-runtime-r2/src-tauri/src/host.rs:4910` reads the current selected provider and returns `(provider.clone(), None)`, bypassing resolve_model_selection. A legacy ProviderConfig may carry model=Some(...): the unguarded configuration contract still permits it. The explicit default resolver clears provider.model at line 3033; the null branch does not, and provider command construction consumes that field at line 3278. Thus a conversation presented as using provider default can execute a saved explicit model, with no admitted model-selection receipt, after this frontend change.

A synthetic frozen controller fixture with snapshot.providers[0].model='unverified-legacy-model' confirms that send is permitted, selection remains null and the fake admitted provider retains that model. This validates the reachable frontend condition; native execution was not performed. Native source independently establishes the clone/command path. This is not a claim that real provider inference occurred.

At host admission, resolve the current inherited route through an explicit defaults-only selection built from the current catalog while leaving the saved conversation selection null, or reject unsupported legacy overrides for explicit review. Preserve admitted resolution provenance and revalidate at admission, rather than relying only on the earlier frontend catalog check. Add a native synthetic regression with a legacy non-null provider.model and a default change between frontend validation and admission; assert no unverified override executes and saved inheritance remains null.

## Accepted portions

Follow action snapshots expected draft revision and default ID, refreshes, rejects observed changes/pending or active work and protects team/explicit model selections. It saves null selection/team through the existing mutation identity, revision CAS and exact durable acknowledgement, with local draft and copied attachment IDs. Uncertain prior saves reject replacement. Native workspace-lock fence and same-mutation replay ordering remain unchanged. Default ID checking occurs before the save call in the controller; the rich-draft native request does not carry a default-ID CAS. Subsequent default changes are intentionally part of inheritance, not a promise to freeze that provider.

Sending validates the current frontend route but preserves null instead of repinning. The host reads its current global default at admission, so later default changes are inherited as intended, subject to the P2 above. Explicit conversation selections and team execution continue through the existing resolver. Attachment preservation here covers the unpin save, not new support for sending attachments through the direct frontend slice.

Independent evidence: exact frozen controller/dependencies/tests in s02-follow-frozen-tests; four owner focused tests passed, plus one reviewer characterization confirming the P2. Run `node --experimental-strip-types --test --test-name-pattern='following default|direct send saves exact|reviewer inherited' qa-artifacts/constellation-state-review-20260909/s02-follow-frozen-tests/tests/hostController.test.mjs`. A passing characterization demonstrates the defect, not acceptance. The explicit-team owner test uses an unconfigured second member; its rejection may arise at snapshot hydration, so it is weaker evidence than a valid-team regression. Production team guard is present and prior valid-team review remains applicable.

Settings close change is source-local: contained SVG is aria-hidden inside the existing named button; Escape handler unchanged; CSS centers it with zero padding. No independent pixel/visual claim is made. Owner measured screenshots and mounted artifacts were not forwarded or rerun. No product edits, native build/launch, real provider/configuration/auth action, publication or private artifact forwarding occurred. Inheritance review only; prior binding/synthesis/discovery acceptances were not reopened.


## Targeted correction acceptance — ae27a875 (2026-09-10)

**Reported inherited-admission P2 closed** in frozen ae27a875aab6bc4d78f05f73e1374d73a5826171 against 6119dad12a2af59bee4f77e7d616e2cdfd76b46d. Reviewed the complete two-file delta, local receipt, unchanged resolver and native admission lock context. Bounded source correction accepted; no new actionable defect found in this correction.

The null-selection branch builds a temporary ModelSelection for the workspace's currently selected provider with null model/effort and the current catalog revision, then calls resolve_model_selection. The resolver validates supported/installed/default-capable route and returns a cloned provider with model cleared. The admitted request now records Some(resolution), including providerManagedDefault, current requested provider/catalog revision, adapter revision and unknown effective model/effort IDs. The temporary selection is not written into the conversation. Shared workspace references plus the resolver's explicit clone preserve saved provider.model and null rich-draft selection. prepare_reserved_submission holds the lifecycle/workspace locks during this resolution, so it uses the native admission state rather than the earlier frontend catalog.

The exact new regression calls prepare_submission without invoking any process. It creates an earlier catalog, then a later selected fixture with a legacy override; asserts later provider, cleared admitted model, recorded defaults provenance/current revision, saved null selection and original legacy provider field unchanged; then makes the executable path missing and asserts rejection. This is a relevant regression for the reported path and would fail before the correction. It checks missing executable rather than removed provider; removal and unsupported kind are separately rejected by the inspected resolver's provider lookup/adapter-state checks. Its error assertion is generic, not an exact code assertion. These coverage limits do not block the localized correction.

The receipt reports one synthetic regression and two capability tests passing; those are owner-reported runs, not independently rerun or independently verified log outputs here. No Rust compilation, frontend/UI repetition, provider process, native launch, product edits, publication or private artifact forwarding occurred. The earlier frontend characterization remains historical: its fake host clones provider config and does not implement this corrected native resolver, so it must not be used to declare the successor defective.

Acceptance covers source behavior and regression design only. Real provider compatibility, authentication, OS/runtime delivery and separate persistence work remain outside this verdict.
